# Chamados de TI — ISV

> Arquivo de contexto para Claude Code. Leia este arquivo inteiro antes de qualquer tarefa.

---

## Visão geral do projeto

PWA de abertura e acompanhamento de chamados de suporte técnico do **Instituto São Vicente (ISV)**. Funciona direto no navegador, sem backend próprio — toda a persistência é feita via **API do ClickUp**. Usuários abrem chamados, acompanham o status e recebem notificações push quando há atualizações.

**Objetivo:** substituir comunicação informal (WhatsApp/verbal) com TI por um canal formal, rastreável e com SLA definido.

**Stack:** HTML + CSS + JS puro (sem framework), PWA com Service Worker, integração direta com a API REST do ClickUp, notificações push via Cloudflare Worker.

---

## Ecossistema (ISV / Desenvolvimento)

Este projeto faz parte da pasta `Desenvolvimento/`, que reúne os sistemas do Instituto São Vicente (ISV). Padrões compartilhados ficam em:

- **Comandos e agentes:** `~/.claude/` — `/atualizar`, `/encerrar`, agente `revisor`
- **Assets/estilos/componentes comuns:** `../shared/`
- **Referência de comandos:** `../COMANDOS-CLAUDE.md`

> ⚠️ **NÃO ler nem indexar as pastas dos outros projetos** (`projeto-*`, `pessoal-*`) a menos que explicitamente solicitado.

---

## Estrutura de arquivos

```
/
├── README.md           ← visão geral e instruções
├── CLAUDE.md           ← este arquivo
├── index.html          ← ⚠️ RETIRADO da produção desde a Fase F5 (2026-08-12) — ver nota abaixo
├── admin.html          ← ⚠️ RETIRADO da produção desde a Fase F5 (2026-08-12) — ver nota abaixo
├── sw.js                ← ⚠️ RETIRADO da produção desde a Fase F5 — ver nota abaixo
├── push-worker.js      ← fonte do Cloudflare Worker de push (deploy automatizado, ver "Deploy do Worker")
├── wrangler.toml       ← config do Worker pro Wrangler (nome, entrypoint, binding do KV) — sem secrets
├── manifest.json       ← ⚠️ RETIRADO da produção desde a Fase F5 — ver nota abaixo
├── css/                  ← ⚠️ RETIRADO da produção — mesma nota
├── js/                   ← ⚠️ RETIRADO da produção — mesma nota
├── tests/
│   ├── app.test.js         ← testes de js/app.js (código retirado da produção, mas os testes continuam valendo/passando — ver nota)
│   ├── push-worker.test.js ← testes do Worker (auth, isolamento entre solicitantes, rotas /admin/*)
│   ├── d1-layer.test.js    ← testes da camada de dados D1 + automação de SLA (Fases B2/B5, node:sqlite)
│   ├── r2-layer.test.js    ← testes da camada de anexos R2 (Fase B4, mock em memória)
│   └── run-all.js          ← roda as 4 suítes de uma vez (`npm test`, Fase B6)
├── d1/
│   └── schema.sql          ← schema do banco D1 (Fase B2) — não usado em produção ainda
├── frontend/                ← ✅ CÓDIGO REAL DE PRODUÇÃO desde a Fase F5 (2026-08-12) — React,
│                               package.json próprio, zero-dependência não se aplica aqui (ver
│                               "Decisões técnicas"). Build MPA: `admin.html`+`index.html` como
│                               entradas de verdade (`vite.config.ts`), publicado via
│                               `.github/workflows/deploy-frontend.yml`
├── package.json             ← só pelo atalho `npm test` (Fase B6) — sem dependência nenhuma
└── assets/
    ├── icon.svg            ← ícone principal (fonte pra frontend/public/, copiado na F4.5)
    ├── icon-maskable.svg   ← ícone adaptável (Android) (idem)
    ├── favicon-isv.png     ← ícone da aba do navegador (idem)
    └── logo-isv.svg        ← logo do instituto (idem, frontend/src/assets/)
```

> ⚠️ **Fase F5 (2026-08-12): corte de produção do frontend feito — `index.html`/`admin.html`/`js/`/
> `css/`/`sw.js`/`manifest.json` na raiz do repo NÃO são mais o que está no ar.** O GitHub Pages
> deste repo foi trocado de "Deploy from a branch" (servia esses arquivos direto) pra "GitHub
> Actions" (`build_type: workflow`) — agora serve exclusivamente o que
> `.github/workflows/deploy-frontend.yml` publica a partir de `frontend/dist`. Os arquivos vanilla
> **continuam no repo de propósito**, intocados, como caminho de rollback rápido (ver "Deploy do
> Frontend" pra como reverter) — não apague sem decisão explícita separada. Os testes de
> `js/app.js`/`push-worker.js` continuam rodando e válidos (o Worker não mudou nada).

---

## Arquitetura e integrações

### D1 + R2 como backend (ClickUp desligada desde a Fase M5, 2026-08-13)
- **Fonte de verdade é o Cloudflare D1** (`chamados-ti-db`, binding `CHAMADOS_DB`) — cada chamado é uma linha na tabela `chamados` (schema em `d1/schema.sql`), com `chamado_assignees` (N-pra-N de operadores) e `chamado_anexos` (metadados de anexo, o arquivo em si vive no R2) como tabelas relacionadas. `solicitantes` é outra tabela própria (nome como chave, sem indireção nenhuma). Anexos (bytes do arquivo) ficam no **Cloudflare R2** (`chamados-ti-anexos`, binding `ANEXOS`), servidos só via rota autenticada (`GET /api/anexos/:id`).
- **A ClickUp não é mais chamada por nenhuma rota** — `push-worker.js` não faz nenhum `fetch()` pra `api.clickup.com`. A migração saiu em 5 fases (M1-M5, 2026-08-13, mesmo dia — ver "Decisões técnicas tomadas" pro detalhe completo de cada uma): lista de solicitantes, anexos, criação de chamado, atualização/automação de SLA e leitura final, nessa ordem. A conta da ClickUp **continua existindo, intacta, como arquivo consultável manualmente** (decisão deliberada, não um cancelamento) — só parou de ser chamada pelo código. `env.CLICKUP_API_KEY` continua configurada no Worker por enquanto (não afeta nada, nenhuma rota lê), removível com `wrangler secret delete` quando quiserem reduzir a superfície.
- **O navegador nunca recebe segredo nenhum de acesso a dado sensível.** Toda chamada do frontend (React, `frontend/src/lib/api.ts`/`app-api.ts`) vai pro Cloudflare Worker (`push-worker.js`, rotas `/api/*`), autenticada por `APP_SHARED_SECRET` num header (`X-App-Secret`) — esse valor é público por design (só libera o uso do Worker, não dá acesso a dado de ninguém por si só; a identidade de quem está pedindo vem sempre da sessão, ver "Autenticação"). Esse mecanismo já existia desde antes da migração (quando o header liberava um proxy pra ClickUp) — continua com a mesma função, só não é mais "proxy" de nada externo.
- **Segurança básica do Worker:** CORS restrito a `ALLOWED_ORIGIN` (só o domínio do GitHub Pages, não mais `*`) e throttle de 60s por solicitante em `/api/tasks` (POST), reaproveitando o KV `SUBSCRIPTIONS` já usado pra sessão/login — evita duplo-clique virar chamado duplicado e freia flood sem precisar de infra nova.
- **Login com senha (pedido da diretoria, 2026-07-24):** ver seção "Autenticação" abaixo — identidade agora é decidida pelo Worker a partir do token de sessão, nunca pelo que o cliente alega.
- 🛡️ **Achado sobre o caminho de rollback do frontend (F5), relevante desde a migração de saída da ClickUp:** o rollback documentado em "Deploy do Frontend" (voltar `build_type` das Pages pra `legacy`, servindo os arquivos vanilla `index.html`/`admin.html`/`js/`/`css/` de novo) assumia implicitamente que o **Worker** continuava igual por baixo — o que deixou de ser verdade a partir da M1. `js/app.js`/`js/admin.js` (vanilla, nunca atualizados desde a F5 de propósito) foram escritos pra um Worker ClickUp-nativo: esperam `GET /api/field` (removida na M5), IDs de chamado no formato da ClickUp (agora são UUID, desde a M3), e não sabem nada sobre `usub_<nome>` como chave de push (mudou na M5). **Reverter só o frontend pro vanilla hoje não reverteria o comportamento pra como era antes da migração** — o Worker que ele chamaria já seria o D1-nativo atual. Registrado aqui pra não ser redescoberto/mal-diagnosticado se um rollback de frontend for cogitado no futuro.

### Autenticação
- **Motivo:** antes, qualquer um podia selecionar o nome de outra pessoa no seletor e ver os chamados dela — o Worker confiava cegamente em qual "solicitante" o navegador dizia ser. A diretoria pediu senha justamente pra impedir isso.
- **Sem banco novo** — reaproveita o mesmo KV `SUBSCRIPTIONS` do Worker, com prefixos de chave diferentes: `auth_<nome>` (senha), `session_<token>` (sessão), `loginfail_<nome>` (contador de tentativas erradas).
- **Senha:** PBKDF2-SHA256 (100.000 iterações), salt aleatório por pessoa. Formato de armazenamento autodescritivo (`{algo, iterations, salt, hash}`) — permite trocar de algoritmo/parâmetros no futuro sem invalidar senha de ninguém (rehash silencioso no próximo login bem-sucedido, se um dia for preciso).
- **Primeiro acesso = cadastro:** se `POST /auth/login` devolve 404 (sem senha cadastrada pra aquele nome), `app.js` chama `POST /auth/register` com a mesma senha digitada — não existe cadastro em separado.
- **Sessão:** token opaco de 256 bits, guardado em `localStorage.session_token`, válido por `SESSION_TTL_SECONDS` (90 dias) e mandado no header `X-Session-Token` em toda chamada a `/api/*` e `/subscribe`.
- **Identidade sempre resolvida no servidor:** `handleGetMyTasks`, `handleCreateTask`, `handleGetTask`, `handleUploadAttachment` e `handleSubscribe` nunca confiam em nenhum índice/nome que o cliente mande — resolvem a partir do `name` gravado na sessão (comparado direto contra `chamado.solicitante` no D1, sem indireção de orderindex nenhuma desde a migração de saída da ClickUp). `handleCreateTask` **sobrescreve** SOLICITANTE **e** recalcula `priority`/`due_date` a partir do TIPO (mesma regra de `CATEGORIA_PRIORIDADE`/`PRIORITY` do frontend, duplicada no Worker) — sem isso, dava pra chamar o Worker direto com o `APP_SHARED_SECRET` (público por design) e forjar prioridade/prazo. `handleGetTask` e `handleUploadAttachment` confirmam que o chamado pertence a quem está logado antes de devolver/anexar (403 se não for dono).
- **Brute-force:** 5 tentativas erradas de senha bloqueiam novos logins daquele nome por 15min (`loginfail_<nome>`, expira sozinho). Senha mínima de 8 caracteres.
- **`hasValidSecret` falha fechado:** se `SUBSCRIBE_SECRET` não estiver configurado no Worker (deploy manual — dá pra esquecer), o proxy fica bloqueado por padrão, não aberto.
- **Esqueceu a senha / nome "roubado":** sem tela de admin — a TI apaga a chave `auth_<nome>` direto no painel do KV da Cloudflare, e a pessoa cadastra de novo no próximo login.
- **Não dá mais pra "trocar de nome"** nas configurações do app — quem você é vem da sessão autenticada. Pra usar como outra pessoa, precisa deslogar e logar com a senha dela.
- **`GET /admin/users`** (2026-07-24) — visão geral de quem já tem senha cadastrada (nome, `createdAt`, `lastLoginAt`; nunca hash/salt), protegida por `env.ADMIN_SECRET` (header `X-Admin-Secret`) — segredo **separado** do `APP_SHARED_SECRET`, nunca fica no app.js/navegador. Não é um painel visual, só um endpoint — a TI consulta via curl (ou uma páginazinha própria, se um dia quiser). Serve pra: acompanhar quem já entrou no rollout, e desconfiar/checar se alguém pode ter cadastrado o nome errado por engano.
- **`GET /admin/tasks` e `GET /admin/metrics`** (2026-08-06, Fase 1 do painel de admin; cortadas pra ler do D1 na B7 parte 2, 2026-08-12) — mesma proteção por `ADMIN_SECRET`/`X-Admin-Secret` de `/admin/users`. `/admin/tasks` devolve todos os chamados (`d1ListChamados`), com filtros opcionais por query string: `status`, `setor` (orderindex), `tipo` (orderindex), `operador` (id do assignee, via `chamado_assignees` — encontra mesmo quando não é o 1º atribuído), `solicitante` (nome, comparado direto contra a coluna — nome inexistente devolve lista vazia, não erro). `/admin/metrics` devolve agregados (`d1GetMetrics`): total por status (`porStatus`), volume por tipo/setor (`porTipo`/`porSetor`, chaves = orderindex — mapear pro nome no painel via `TIPOS`/`SETORES` do frontend), % dentro do SLA vs atrasado (`sla`, comparando `due_date` com `date_closed` pra encerrados ou "agora" pra tasks ainda abertas) e tempo médio de atendimento por operador (`tempoMedioPorOperador`, calculado como `date_closed - start_date` dos chamados encerrados, agrupado pelo `id` de cada assignee — cada operador atribuído recebe a duração inteira, não dividida).
- **`POST /admin/tasks/:id`** (2026-08-07; virou D1-nativa na Fase M4, 2026-08-13) — única rota de admin que **muta** dado de chamado; mesma proteção `ADMIN_SECRET`. Body aceita qualquer subconjunto de `{ status, solucao, assigneeId }`: `status` chama `d1TransitionStatus` (síncrona, na mesma request — já cobre pausa/retomada de SLA, `start_date`/`due_date`/`date_closed` e push, sem depender de webhook nenhum); `solucao`/`assigneeId` viram `d1UpdateChamado` direto (`assigneeId` substitui por completo quem está em `chamado_assignees`, no mesmo `.batch()` que `assignee_id` — não precisa mais de diff `{add, rem}`, isso só existia por causa da API da ClickUp); `assigneeId: null` remove todo mundo que estava atribuído ("Sem atribuição" no painel). Sem transação real entre as 3 sub-mutações (cada uma é sua própria escrita) — uma falha no meio reporta em `updated` o que já tinha sido aplicado antes de parar.

### Painel de admin (`admin.html`/`admin.js`, Fase 2)
- Página separada do app principal, fora do `manifest.json`/`sw.js` (não é instalável, não é PWA) — só a TI acessa, direto pela URL do GitHub Pages (`/admin.html`).
- **O `ADMIN_SECRET` nunca é hardcoded em `admin.js`** — diferente do `APP_SHARED_SECRET` (que é público por design), o segredo de admin dá acesso a dados de todo mundo, então não pode estar em código público no GitHub Pages. Em vez disso: tela de "gate" pede o segredo, valida com um `GET /admin/users` (chamada leve, só lê o KV) e, se aceito, guarda em `localStorage.admin_secret` **do navegador de quem digitou** — nunca no código. Toda chamada a `/admin/*` manda esse valor no header `X-Admin-Secret`. Se o Worker responder 403 em qualquer chamada (segredo trocado/revogado), `admin.js` limpa o `localStorage` e volta pro gate.
- Consome `GET /admin/tasks` (tabela com filtros de status/setor/tipo/operador/solicitante) e `GET /admin/metrics` (cards de total por status, % dentro do SLA vs atrasado, volume por tipo/setor, tempo médio de atendimento por operador, gráfico donut de SLA em SVG puro). Lista de solicitantes pro filtro é buscada em runtime da ClickUp (`GET /api/field`, mesmo `APP_SHARED_SECRET` público do app principal — não precisa do `ADMIN_SECRET` pra isso).
- **Busca por título e exportação CSV são client-side** — sobre o resultado que o servidor já filtrou (`allTasks` em `admin.js`), sem endpoint novo nem nova chamada ao Worker a cada tecla digitada. CSV exporta o conjunto visível (filtros + busca), com BOM UTF-8 pra acentuação abrir certo no Excel, e prefixa com `'` qualquer célula que comece com `=`, `+`, `-`, `@` ou tab/CR — proteção contra "formula injection" (`task.name` é texto livre de qualquer solicitante; sem isso, um título de chamado como `=HYPERLINK(...)` viraria fórmula executável ao abrir o CSV no Excel).
- **Tabela agrupada por status, com expandir/recolher** (2026-08-07, pedido explícito pra ficar parecido com a própria tela da ClickUp) — substituiu a paginação global de 25/página: cada status (Aberto/Em Atendimento/Pendente/Encerrado) é uma seção própria, com ícone (círculo vazado pra Aberto, círculo com check colorido pros demais — mesmo padrão visual da ClickUp), contador e chevron que gira ao recolher (`groupCollapsed` em `admin.js`, lembra o estado entre re-renders da sessão). Sem coluna de Status na linha da task (fica implícito pelo grupo). Teto de 200 linhas por grupo (`GROUP_TABLE_LIMIT`), mesma ideia do teto de card do Quadro.
- **Prioridade e Tipo com a mesma linguagem visual da ClickUp** — Prioridade como bolinha colorida + label (reaproveita `.prio-dot` de `css/style.css`, já usado no app principal; cores vêm de `task.priority.priority`, valor bruto da ClickUp: urgent/high/normal/low). Tipo como chip colorido (`.tipo-chip`), reaproveitando as mesmas cores já usadas nos gráficos de "Volume por tipo".
- **Deixou de ser só leitura em 2026-08-07** — decisão do gestor: a TI (Everson/Henrique) para de abrir a ClickUp pra trabalhar e passa a aceitar chamado, mudar status, escrever solução e atribuir operador direto pelo painel (ver `POST /admin/tasks/:id` acima). A ClickUp **continua guardando o dado** (nenhuma migração, nenhuma automação de SLA reescrita) — só deixou de ser a *interface* de trabalho. Cada linha/card tem um botão "Gerenciar" que abre um modal (descrição, status/operador/solução) pré-preenchido com o estado atual do chamado. Se o chamado tiver 2+ operadores atribuídos, o modal avisa (só dá pra pré-selecionar 1 no campo) — e só manda `assigneeId` no `POST` se o admin realmente tocar nesse campo (`operadorTouched` em `admin.js`), senão os outros atribuídos ficam intactos (correção de 2026-08-07: antes disso, salvar qualquer campo do modal apagava silenciosamente um segundo operador já atribuído).
- **Navegação lateral (sidebar) recolhível, separando Gestão de Dashboard** (2026-08-07) — protótipo de navegação pra uma futura evolução em CRM completo (ver "Decisões técnicas tomadas" e a memória `future-crm-stack-decision`). **Gestão** (filtros, Quadro/Tabela, modal "Gerenciar") e **Dashboard** (métricas/gráficos) são seções separadas — filtros só existem em Gestão, já que `/admin/metrics` nunca dependeu de filtro. Estado (seção ativa, sidebar recolhida ou não, view Quadro/Tabela, grupos recolhidos na Tabela) persiste em `localStorage` entre sessões.
- **Quadro (Kanban) é a visão padrão** dentro de Gestão, com **arrastar-e-soltar entre colunas** — mover um card muda só o status (mesma rota `POST /admin/tasks/:id`, sem tocar operador/solução), convivendo com o clique-abre-modal (um drag de verdade não dispara `click` no navegador). Tabela fica a um clique, útil pra buscar/exportar muita coisa de uma vez. Cores dos ícones de status (`STATUS_MAP` em `admin.js`/`app.js`) foram confirmadas direto na configuração real da lista na ClickUp — "Aberto" é cinza (cor padrão do tipo "open" da própria ClickUp), não uma cor customizada.
- **`fetchAllTasks` (push-worker.js) tem teto de 20 páginas (~2000 chamados)** e devolve `truncated: true` em `/admin/tasks`/`/admin/metrics` se bater nesse teto — o painel mostra um banner de aviso quando isso acontece, em vez de mostrar dado incompleto sem avisar. `/admin/tasks` e `/admin/metrics` chamam essa função de forma independente (double-fetch); aceitável no volume atual, mas documentado como limitação conhecida pro dia que o volume crescer (ver comentário no código — a solução seria cachear por 15-30s no KV).
- **`ADMIN_SECRET` também tem lockout por IP** (`adminfail_<ip>`, 5 tentativas erradas / 15min, mesmo padrão do `loginfail_<nome>` de usuário) — só conta como tentativa quando o header `X-Admin-Secret` é mandado com valor errado; requisição sem esse header não conta (senão qualquer chamada "esquecendo" o header já contribuiria pro lockout).
- **Alerta de chamado novo (2026-08-13)** — badge/sino flutuante no header (contagem real de chamados "aberto"), som (Web Audio API) e push do navegador quando um chamado é criado; caixa com botão "Aceitar chamado" (`POST /admin/tasks/:id`, mesma rota do modal "Gerenciar"). Recria o número flutuante que a ClickUp dava, agora que a TI não abre mais ela pra nada. Ver "Notificações push" e "Decisões técnicas tomadas" pro detalhe completo (arquitetura de Service Worker separado, inscrição por dispositivo já que o admin não tem login por pessoa).

### Notificações push
- Arquitetura: browser → Cloudflare Worker (`chamados-ti-push.tecnologiadainformacao-isv.workers.dev`) → Web Push.
- **Lado solicitante**: `d1TransitionStatus` (síncrono, desde a Fase M4) manda push quando o status de um chamado muda pra "em atendimento"/"pendente"/"encerrado" (`NOTIFY_STATUSES`) — lê a inscrição em `usub_<nome>` no KV, gravada por `POST /subscribe` (autenticado por sessão).
- **Lado admin (2026-08-13)**: `handleCreateTask` manda push pra TI quando um chamado NOVO é criado — inscrição por navegador/dispositivo (`adminsub_<id>`, `POST /admin/subscribe`, protegido por `ADMIN_SECRET`, já que não há login por pessoa no admin). Ver "Painel de admin" e "Decisões técnicas tomadas" pro detalhe completo (badge/som/caixa de aceitar).
- `VAPID_PUBLIC_KEY`/`APP_SHARED_SECRET` hardcoded no frontend (não são credenciais de acesso a dado sensível — só liberam o uso do Worker, a identidade de quem está pedindo vem sempre da sessão/segredo de admin).

### Mapeamento de dados (app.js)
| Constante | O que é |
|---|---|
| `TIPOS` | 8 categorias de chamado com prioridade automática |
| `SETORES` | 9 setores da organização |
| `PRIORITY` | 3 níveis: Urgente (1h SLA), Alta (4h), Normal (24h) |
| `CATEGORIA_PRIORIDADE` | Mapeamento categoria → prioridade automática |
| `OPERADORES` | IDs ClickUp dos operadores de TI (Everson, Henrique) |
| `STATUS_MAP` | 4 status: aberto, em atendimento, pendente, encerrado |

---

## Deploy do Worker

- **Automatizado desde 2026-08-10** — substitui o processo antigo (colar `push-worker.js` inteiro no editor do dashboard da Cloudflare, sem CI/CD, que já causou mais de um "colei a versão errada" ao longo do projeto). Agora: `git push` na `main` que toque em `push-worker.js`, `wrangler.toml` ou no próprio workflow dispara `.github/workflows/deploy-worker.yml`, que roda `wrangler deploy` via `cloudflare/wrangler-action@v3`. Também pode ser disparado manualmente em Actions → "Deploy Cloudflare Worker" → Run workflow (`workflow_dispatch`).
- **Autenticação do pipeline:** `CLOUDFLARE_API_TOKEN` (token com o template "Edit Cloudflare Workers") e `CLOUDFLARE_ACCOUNT_ID`, guardados como secrets do repositório no GitHub (`gh secret set`) — nunca em arquivo versionado.
- **`wrangler.toml` não declara nenhum secret do Worker** (`CLICKUP_API_KEY`, `SUBSCRIBE_SECRET`, `ADMIN_SECRET`, `VAPID_PRIVATE_JWK`) — só `name`, `main` e o binding do KV `SUBSCRIPTIONS` (com o ID real da conta). Esses secrets continuam geridos manualmente (dashboard da Cloudflare ou `wrangler secret put`, uma vez só) — `wrangler deploy` nunca os toca, então não há risco do deploy automático apagar/sobrescrever nenhum deles.
- Testado de ponta a ponta em 2026-08-10: push → workflow disparado automaticamente → `wrangler deploy` publicado com sucesso → Worker respondendo em produção com os secrets intactos (confirmado via chamada real a `/admin/users`).

## Deploy do Frontend

- **Automatizado desde 2026-08-12 (Fase F5)** — `git push` na `main` que toque em `frontend/**` (ou no próprio workflow) dispara `.github/workflows/deploy-frontend.yml`: `npm ci` + `npm run build` (Vite, build MPA — ver abaixo) em `frontend/`, depois publica `frontend/dist` via `actions/upload-pages-artifact` + `actions/deploy-pages`. Também pode ser disparado manualmente em Actions → "Deploy Frontend (GitHub Pages)" → Run workflow.
- **GitHub Pages deste repo é um project site** (`https://tecnologiadainformacaoisv.github.io/chamados-ti-pwa/`, não um domínio próprio) — os assets do build precisam do prefixo `/chamados-ti-pwa/`, senão pedem na raiz errada e dão 404. `frontend/vite.config.ts` resolve isso com `base` condicional (`'/chamados-ti-pwa/'` só no build de produção; `'/'` em dev/preview local, sem mudar o fluxo de trabalho do dia a dia).
- **Build MPA (multi-page app)** — `admin.html` e `index.html` são duas entradas HTML de verdade (`build.rollupOptions.input` em `vite.config.ts`), cada uma com seu próprio `main-*.tsx` (`src/main.tsx` monta `SolicitanteApp`, `src/main-admin.tsx` monta `AdminApp`) — produz dois bundles JS separados, como duas páginas de fato independentes (mesma decisão de arquitetura já registrada: "mesma estrutura de hoje, uma página por app"). O switcher de dev por hash (`App.tsx`) foi removido — não precisa mais, cada entrada já roda o app certo nativamente em dev/build/preview.
- **`admin.html` nunca deve ser PWA** (documentado desde a F4.5) — mas o `vite-plugin-pwa` injeta `<link rel="manifest">` em toda entrada HTML do build MPA sem opção nativa de escopar por página. `frontend/scripts/fix-admin-html.mjs` roda como último passo do `npm run build` e remove essa tag só de `dist/admin.html`.
- **⚠️ Achado real, não documentado em lugar nenhum antes de acontecer aqui:** rodar `actions/deploy-pages` com sucesso **publica o site imediatamente como conteúdo servido de verdade — independente do `build_type` configurado no repositório.** A expectativa era que isso só aconteceria depois de trocar manualmente o source das Pages de "Deploy from a branch" (`legacy`) pra "GitHub Actions" (`workflow`) via API/dashboard — mas o primeiro push desta fase, feito só pra validar que o build passava no CI, **já cortou produção de verdade** na hora (confirmado via `Last-Modified` do HTML servido e via `<script src>` mudando pro bundle do Vite). A configuração do `build_type` só decide quem tem permissão de *disparar* um build automático (branch vs. workflow) — não qual deployment está *ativo*; um deployment via Actions sempre vence. **Lição pra qualquer corte de produção futuro neste repo (inclui a B7 dentro do Worker, se algum dia usar um mecanismo parecido): tratar "buildar no CI" e "publicar de verdade" como o mesmo evento, não dois passos separados — não existe uma etapa de build "sem risco" quando o deploy final usa `deploy-pages`.**
- **Corrigido na mesma hora:** trocado o `build_type` das Pages pra `workflow` via `gh api --method PUT .../pages -f build_type=workflow`, deixando a configuração consistente com o que já estava sendo servido — sem isso, um push futuro que só tocasse em outro arquivo (ex.: um fix no Worker) poderia disparar o build legado de novo e reverter o site pro vanilla sem ninguém pedir.
- **Como reverter (rollback), se algum dia precisar:** os arquivos vanilla (`index.html`/`admin.html`/`js/`/`css/`/`sw.js`/`manifest.json` na raiz) continuam intactos no repo de propósito. Trocar `build_type` de volta pra `legacy` (`gh api --method PUT .../pages -f build_type=legacy`) faz o GitHub voltar a auto-buildar da branch a cada push — um push (mesmo vazio, `--allow-empty`) na `main` depois disso re-publica o vanilla como conteúdo ativo, revertendo o corte.
- Testado de ponta a ponta em 2026-08-12: push → workflow disparado automaticamente → build MPA publicado com sucesso → site em produção respondendo com dado real (452 chamados reais carregados no Quadro/Tabela/Dashboard do admin, lista real de solicitantes na tela de login) — sem erro de console em nenhuma das duas páginas.

---

## Fluxo do usuário

1. **Login** — usuário seleciona seu nome (lista buscada em runtime do campo customizado SOLICITANTE na ClickUp) e digita sua senha. Primeira vez pra aquele nome: a senha digitada vira a senha de acesso (sem tela de cadastro separada). Não pede mais código de acesso — a chave da ClickUp nunca chega ao navegador (ver "ClickUp como backend" e "Autenticação").
2. **Tela principal** — exibe "Abrir Chamado" e "Meus Chamados" / "Meu Histórico".
3. **Abertura de chamado** — formulário com tipo, setor, descrição, anexo opcional (limite 10 MB). Ao submeter, cria task no ClickUp com prioridade automática baseada no tipo.
4. **Acompanhamento** — filtra as tasks do ClickUp pelo campo SOLICITANTE. Exibe status com SLA e indicação de atraso.
5. **Notificações** — ao autorizar, o browser se inscreve via `push-worker.js`; o Cloudflare Worker envia push quando o status muda.

---

## Regras de negócio que não devem ser alteradas sem perguntar

- **Prioridade é automática** — definida pelo tipo do chamado via `CATEGORIA_PRIORIDADE`. Não expor seleção manual de prioridade ao usuário.
- **Prioridade "Baixa" não existe** — nenhuma categoria mapeia para ela; o ClickUp a ignora neste contexto.
- **SLA exibido** é informativo (calculado no cliente com `task.due_date`); quem define o `due_date` real é o ClickUp via automação (`push-worker.js`). Existem **duas fases de prazo distintas**, não uma só:
  - **Aceitação** (Aberto → Em Atendimento): contada da criação do chamado. Urgente 1h / Alta 4h / Normal 24h — vem de `PRIORITY.slaMs` em `app.js`.
  - **Finalização** (Em Atendimento → Encerrado): contada do momento em que o operador marca "Em Atendimento" (não da criação). Se a task tiver `time_estimate` preenchido manualmente na ClickUp, ele tem prioridade; senão, cai no padrão por prioridade em `DEFAULT_TIME_ESTIMATE_MS` (`push-worker.js`): **Urgente 15min / Alta 30min / Normal 1h**. "Baixa" não tem padrão (prioridade não usada pelo app).
- **Lista de solicitantes é buscada em runtime direto do campo customizado SOLICITANTE na ClickUp** (`loadSolicitantes()` em `app.js`) — não existe mais array fixo no código. Adicionar/renomear alguém só na ClickUp já é suficiente; não precisa mais editar nem publicar o app. `localStorage.user_name` guarda o nome (string), não mais um índice numérico. Existe uma tabela de migração (`LEGACY_USER_IDX_TO_NAME`) só pra traduzir o índice antigo de quem configurou o app antes da v0.2.5 — essa tabela é histórica e não deve ser editada.
- **Sincronização de status:** as chaves de `STATUS_MAP` em `app.js` devem ficar idênticas a `NOTIFY_STATUSES` em `push-worker.js`.
- **Sincronização de campo:** o field_id de `SOLICITANTE` deve ser idêntico em `FIELD_IDS` (app.js) e em `push-worker.js`.
- **Limite de anexo:** 10 MB por arquivo — validado no cliente antes do upload.

---

## Padrões de desenvolvimento

- Versionamento: **Semantic Versioning** (`MAJOR.MINOR.PATCH`); `MAJOR` = 0 enquanto pré-produção — **saiu do 0 em 2026-08-13, na conclusão da migração de saída da ClickUp (v1.0.0, ver abaixo)**.
- Commits: **Conventional Commits** (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`).
- **Versão atual declarada em dois lugares** — manter sincronizados: **`frontend/package.json` (campo `version`)** e **tags git** (`vX.Y.Z`, anotadas). Convenção mecânica desde 2026-08-13: todo commit `feat:` sobe MINOR, todo `fix:` sobe PATCH, `docs:`/`chore:`/`refactor:` não sobem versão sozinhos (ficam sob a versão do commit anterior). O `<meta name="app-version">` do `index.html` vanilla **não é mais o local declarado** — ficou congelado em `0.3.5` desde antes da Fase F5 (frontend React virou produção) e nunca foi atualizado depois disso; **histórico, não vivo** (o arquivo em si continua existindo só como caminho de rollback, ver "Estrutura de arquivos").
- Não introduzir frameworks ou bundlers sem decisão explícita — o projeto é propositalmente zero-dependência.
- Não usar `innerHTML` com dados do usuário sem escapar via `escHtml()`.

---

## Contexto organizacional

- **Organização:** Instituto São Vicente (ISV)
- **Responsável de TI:** Henrique (TI — ISV)
- **Operadores de TI no ClickUp:** Everson (`170628721`) e Henrique (`200498355`)
- **Público-alvo:** colaboradores administrativos, assistenciais e de suporte da ISV

---

## Estado atual do desenvolvimento

> Última atualização: 2026-08-17

- **🐛 Scrollbar visível no app do solicitante, corrigido (2026-08-17)** — usuário reportou print apontando a barra de rolagem do navegador visível na lateral direita e pediu pra remover, notando "isso estava no projeto antigo" (achado confirmado: `css/style.css` sempre escondeu a scrollbar globalmente — `scrollbar-width: none` + `::-webkit-scrollbar { display: none }`, linhas 41-42 — regra nunca portada quando o app do solicitante virou React na Fase F1/F4, 2026-08-11). Corrigido em `frontend/src/index.css`, escopado por uma classe nova no `<body>` de `frontend/index.html` (`app-solicitante`) — **de propósito não aplicado no admin** (`admin.html` não ganha essa classe): o painel tem tabela/Kanban longos onde a barra é um sinal útil pra quem trabalha ali; o app do solicitante nunca precisou dela (o scroll em si continua funcionando, só a barra visual some, mesmo comportamento do app antigo). Verificado com Playwright: largura ocupada pela scrollbar cai a 0px no app do solicitante, admin permanece com a barra nativa intacta.

- **🐛 Texto livre sem espaço estourava modais/cards em todo o front, corrigido (2026-08-17)** — usuário reportou print de produção: o popup "Chamado novo" do admin com o título do chamado (string sem espaço nenhum, tipo teste/spam) vazando pra fora do modal inteiro. Causa raiz: `overflow-wrap: break-word` (`break-words`) só evita que o TEXTO force a largura — não evita que um container pai (item de `flex`/`grid` sem `min-w-0`) continue calculando sua largura mínima pelo conteúdo intrínseco ("grid/flex blowout" clássico de CSS). Corrigido em duas camadas:
  - **Primitivos compartilhados** (`ui/dialog.tsx`, `ui/alert.tsx`) — `DialogTitle` ganhou `break-words`, `DialogHeader` ganhou `min-w-0` (sem isso o `break-words` do título sozinho não bastava — quem tranca a largura errada é o container pai, não o texto), `AlertDescription` ganhou `min-w-0 break-words` (usada em 9 lugares pra mensagem de erro, exatamente o tipo de texto com URL/id/stack sem espaço). Corrige de uma vez todo `DialogTitle`/`Alert` do app, presente e futuro.
  - **Componentes específicos**: `novo-chamado-alert.tsx` (título/descrição do popup — o bug relatado), `task-modal.tsx` (descrição, timeline de notas), `kanban-board.tsx` (título do card + a COLUNA em si, que também é item de `grid xl:grid-cols-4` sem `min-w-0`), `ticket-card.tsx` (título/descrição/solução — a tela mais vista por usuários reais), chips de anexo em `ticket-card.tsx`/`novo-chamado-form.tsx` (nome de arquivo livre), `filters-bar.tsx` (nome de solicitante no filtro), `tasks-table.tsx` (coluna Solicitante).
  - **Hardening adicional**: campo "Detalhes adicionais" (`novo-chamado-form.tsx`) nunca teve limite de tamanho (diferente de "Descrição do problema", já capado em 300) — ganhou `maxLength={1000}` + contador, reduzindo o pior caso na origem.
  - Achado via `revisor` numa varredura sistemática pedida pelo usuário ("organize este front") — achou os 2 problemas nos primitivos compartilhados que uma correção só nos componentes específicos teria deixado passar (2 dos 4 dialogs do app continuariam quebrados). Verificado com Playwright usando a mesma string patológica do print real (300 caracteres sem espaço) em 4 superfícies (card do Quadro, modal "Gerenciar" com erro de save, card do solicitante) — bounding box de cada uma confirmado dentro do viewport, zero vazamento. 213 testes de backend inalterados (mudança 100% frontend).

- **🎨 Reorganização do formulário "Novo Chamado" a pedido do usuário (2026-08-15)** — usuário pediu explicitamente: "Setor" ao lado de "Solicitante", e "Tipo de solicitação" numa linha própria abaixo, alinhada com o resto do formulário ("preciso de simetria"). `novo-chamado-form.tsx`: Solicitante e Setor viraram a primeira linha (grid de 2 colunas, mesma largura pros dois); Tipo de solicitação desceu pra sua própria linha, largura cheia, com as mesmas bordas esquerda/direita de Descrição/Detalhes/Anexo/botão — antes ficava dividindo espaço com Setor numa segunda linha, sem alinhamento consistente com o resto do card.

- **🐛 Select de "Tipo de solicitação" estourava o card (grid blowout), corrigido (2026-08-15)** — usuário reportou print com o texto do dropdown passando da área visível, tanto no gatilho fechado (texto cortado sem reticências, caixa mais larga que o card) quanto no painel aberto (opções deslocadas pra fora da área visível). Causa: `SelectTrigger` do shadcn é `w-fit` por padrão, e a coluna do grid (`novo-chamado-form.tsx`, "Setor"/"Tipo de solicitação" lado a lado) não tinha `min-w-0` — um item de grid nunca encolhe abaixo do conteúdo intrínseco sem isso ("grid blowout" clássico de CSS), então a opção mais longa de `TIPOS_FULL` ("Plataformas (Google Drive, OMIE, Domínio e ZAPPY)") estourava a coluna. Corrigido com `min-w-0` nas duas colunas + `w-full` nos dois `SelectTrigger` (Setor também, por consistência) — texto agora trunca com reticências dentro da largura certa, e o painel abre alinhado ao card, sem vazar pra fora.

- **🛡️ Auditoria do agente `revisor` (2026-08-14) sobre os 5 commits acumulados desde a última revisão** (logo, Fase A, Fase B, fix visual 2ª rodada) — nenhum achado crítico. 2 avisos e 2 sugestões, todos corrigidos no mesmo dia:
  - 🟡 **Seleção da ação em lote (`tasks-table.tsx`) ficava "fantasma" ao trocar filtro/refetch** — o `Set` de ids selecionados não reconciliava com a lista de chamados visível; selecionar linhas, mudar um filtro que as esconde, e aplicar uma ação em lote nesse estado afetaria chamados que o usuário não estava mais vendo. Corrigido com um `useEffect` que poda `selected` toda vez que `tasks` muda.
  - 🟡 **Erro ao adicionar nota (`task-modal.tsx`) era engolido em silêncio** — só sessão expirada (403) era tratada; qualquer outra falha (rede, 500) deixava o botão só voltar a ficar clicável, sem indicar que a nota não foi salva. Corrigido com o mesmo padrão de `Alert` que o resto do modal já usa.
  - 💡 **Empate de `created_at` na timeline (`chamado_eventos`) tinha ordem indefinida** — dois eventos no mesmo milissegundo (ex.: status e operador mudando na mesma request) não tinham desempate. Corrigido com `ORDER BY created_at, rowid` (rowid é implícito em qualquer tabela SQLite normal e cresce na ordem de inserção — sem precisar de migração de schema).
  - 💡 **Proteção de "status desconhecido" no select inline da Tabela** — avaliado e não corrigido: `byStatus` (que agrupa a Tabela) já descarta silenciosamente qualquer chamado com status fora dos 4 conhecidos antes de chegar em `TaskRow` (comportamento pré-existente, não introduzido pela Fase A) — o caminho que a sugestão descrevia não é alcançável na prática nesta view.
  - 213 testes de backend no total (53+112+41+7, +1 novo cobrindo o desempate de `rowid`), 0 falhando. `tsc -b`/build limpos. Verificado com Playwright (rede mockada): barra de ação em lote some corretamente quando a seleção sai da lista visível por causa de um filtro; alert de erro aparece ao simular falha no `POST /admin/tasks/:id/eventos`.

- **🎨 Regressão visual da F5 — 2ª rodada, achados novos (2026-08-14)**: usuário reclamou de novo que o app do solicitante estava "pelado"/branco, dessa vez mostrando prints concretos (tela "Novo Chamado" e a caixa de sucesso do WhatsApp). O fix de 12/08 (ver entrada mais antiga abaixo) só tinha coberto o **fundo de tela** (gradiente do login/headers) — nunca chegou nos **componentes de conteúdo**, que carregavam boa parte da cor/identidade e ficaram genéricos desde que nasceram na Fase F4 (11/08), fora do escopo daquele fix. Investigado comparando `frontend/src/` linha a linha contra `css/style.css` (nunca por suposição). 5 achados, todos corrigidos:
  1. **Cabeçalho colorido do card sumiu inteiro** — vanilla tinha uma faixa em gradiente navy com "Novo Chamado" em branco no topo do card (`.form-card-header`), nunca portada. `novo-chamado-form.tsx` ganhou essa faixa de volta (reaproveitando `.bg-brand-gradient`, já existente).
  2. **Fundo da página muito mais claro que o original** — vanilla usava `#f0f4f8` (cinza-azulado visível); a versão React usava `bg-muted/30`, que lava quase pra branco puro. Nova classe `.bg-app-shell` (`index.css`) com o valor exato do vanilla, aplicada só em `SolicitanteApp.tsx` — **de propósito não mexida no admin**, que usa `bg-muted/30` sem essa queixa.
  3. **Botão "Abrir Chamado" não era largura-cheia/centralizado** — vanilla usava `.btn-full` (100%); a versão React usava `self-start` (cola na esquerda). Trocado pra `w-full justify-center`. O degradê do botão (`button.tsx`) já existia desde o fix de 12/08, só ficava imperceptível num botão pequeno colado na esquerda — não precisou mexer na cor, só no tamanho/posição.
  4. **Abas sem nenhuma cor de marca** — `tabs.tsx` é o único componente de `ui/` que nunca tinha levado o tratamento de marca que `button.tsx`/`sidebar.tsx` já tinham. Item ativo trocou de `bg-background`/`text-foreground` genérico pra `bg-primary/10`/`text-primary` — **seguro mexer no primitivo compartilhado** porque `<Tabs>` só é usado pelo app do solicitante hoje (confirmado por grep antes de editar), zero risco pro admin. Contadores das abas ("Meus Chamados (3)") viraram pills coloridas (nova classe `.count-pill`, mesmo valor do `.count-pill` vanilla) em vez de texto solto entre parênteses.
  5. **Modal de sucesso do WhatsApp era um Dialog genérico** — `wa-modal.tsx` reconstruído pra ter o ícone verde circular de sucesso, a pill colorida do prazo de SLA, texto centralizado, e os dois botões empilhados largura-cheia (CTA "Abrir WhatsApp" em cima, "Fechar" embaixo) — mesmo padrão do `.wa-modal-card` vanilla.
  - Verificado com `tsc -b`/build limpos e Playwright (rede mockada, fluxo completo: preencher formulário → abrir chamado → checar o modal do WhatsApp) — screenshot de cada tela confirmando visualmente contra a descrição do vanilla, zero erro de console. Nenhum teste de backend afetado (mudança 100% frontend, 212 testes continuam passando).

- **🚀 Painel de admin — Fase B concluída (2026-08-14, mesmo dia da Fase A e do MVP visual): histórico + comentários por chamado, e ação em lote — via `POST /admin/tasks/bulk` (nova rota).** Usuário pediu pra seguir direto pra essa fase depois da A ("prossiga direto para a fase 2, com auto accept"). Os dois itens que exigiam tabela nova no D1 (identificados no planejamento da Fase A):
  - **`chamado_eventos`** (`d1/schema.sql`, `POST /admin/migrate-schema-chamado-eventos`) — timeline única por chamado misturando eventos automáticos (`tipo: 'status'`/`'operador'`, `de_valor`/`para_valor`) com notas manuais da TI (`tipo: 'nota'`, `autor`/`texto`) — mesmo conceito já validado no Artifact do MVP visual. `applyAdminTaskUpdate` (extraída de `handleAdminUpdateTask` numa função pura, reaproveitada por ambas as rotas) grava um evento automático toda vez que status/operador muda **de verdade** (não grava à toa se o valor "muda" pro mesmo que já era). `GET/POST /admin/tasks/:id/eventos` — a nota exige `autor` (texto livre; sem login por pessoa no admin, o painel usa um `<Select>` de `OPERADORES` mas o servidor só exige não-vazio) e `texto`. Frontend: em vez de um componente de drawer novo, o modal "Gerenciar" (`task-modal.tsx`) já existente foi estendido com a timeline + composer — mesmo `Dialog`, mais conteúdo, evita fragmentar a interação já conhecida por quem usa o painel.
  - **Ação em lote** (`POST /admin/tasks/bulk`) — mesmo subconjunto de campos de `POST /admin/tasks/:id` (`status`/`solucao`/`assigneeId`), aplicado a vários chamados de uma vez via a mesma `applyAdminTaskUpdate` (sem duplicar validação/lógica). Sem transação entre chamados — reporta sucesso/falha por id (`{total, sucesso, falha, results}`), mesma filosofia de "não mascarar estado parcial" já usada na rota single-id. Teto de 50 ids por chamada, decisão deliberada citando a própria lição desta sessão sobre orçamento de subrequests por invocação do Worker (não esperar descobrir o teto na marra em produção). Frontend: `tasks-table.tsx` ganhou checkbox por linha + checkbox "selecionar grupo" no cabeçalho de cada status, com uma barra flutuante (mesmo espírito do Artifact do MVP visual) que aparece com a seleção — dois `Select`s (Mudar status / Reatribuir) aplicam na hora.
  - **Decisão deliberada de escopo, não um esquecimento**: o terceiro item do roadmap original da Fase B — "operadores como tabela no D1" (hoje `OPERADORES` é um objeto hardcoded em `constants.ts`, replicado em `push-worker.js`) — foi **avaliado e adiado**, não implementado nesta rodada. Motivo: 25 usos espalhados entre frontend (admin E solicitante) e backend, incluindo `OPERADOR_WHATSAPP` (rota de WhatsApp pro operador atribuído — funcionalidade sensível do app do solicitante) — blast radius real e sem ganho imediato (só 2 operadores hoje, Everson/Henrique; o valor de tornar dinâmico só aparece se/quando a TI crescer). Registrado como próximo passo real, não descartado — ver "Próximos passos".
  - 194 testes de backend novos incluídos (212 no total: 53+112+40+7), 0 falhando. `tsc -b`/`npm run build` limpos. Verificado com Playwright (rede mockada): timeline mista (evento automático + nota) renderizando corretamente no modal, composer postando `{autor, texto}` certo, seleção múltipla + barra de ação em lote postando `{ids, status}` certo em `/admin/tasks/bulk`. **Achado real de tooling durante a verificação** (não é bug do app): `[data-slot="select-trigger"]` sem escopo batia com selects da barra de filtros por trás do modal (Radix Dialog não desmonta o resto da página) — os primeiros scripts de verificação clicavam no seletor errado; corrigido escopando a busca a `[role="dialog"] [data-slot="select-trigger"]`.

- **🚀 Painel de admin "robusto como a ClickUp" — MVP visual + Fase A concluída (2026-08-14)** — usuário mandou print da própria interface da ClickUp e pediu uma evolução visual/funcional do admin nesse espírito, agindo eu como projetista de software: analisar antes de mudar, montar um MVP visual, e só então propor/implementar um roadmap real. Fluxo completo: `EnterPlanMode` → agente Explore levantando o estado atual (componentes, tokens de cor, capacidade de backend já pronta) → 3 perguntas de esclarecimento (`AskUserQuestion`) sobre escopo de IA/navegação tipo workspace (deixado em aberto, não implementado), comentários/histórico (prioridade alta, mas fica pra uma Fase B — exige tabela nova) e edição inline (sim) → **Artifact publicado** (`https://claude.ai/code/artifact/39cb6632-50a3-44ff-ae8c-8045c3ce71c7`, mockup HTML autocontido, dados fictícios, reaproveitando a paleta/tokens reais do projeto — navy/âmbar, cores de tipo/setor/status — não a paleta literal da ClickUp) demonstrando quadro/tabela com pills coloridas, edição inline de status/operador, ação em lote, e um drawer de detalhe com conceito de histórico/comentários. Achado real ao construir o mockup: o menu de reatribuir operador inline renderizava como botões numa linha em vez de dropdown vertical (CSS do `.op-menu-item` nunca tinha sido escrito) — corrigido antes de publicar.
  - **Fase A implementada no mesmo dia** (itens sem migração de schema, aprovados via `EnterPlanMode` de novo — plano detalhado lendo o código real antes de propor). Duas decisões de escopo tomadas nesse planejamento, uma minha e uma do usuário: **"editar descrição" foi removida do escopo** (eu mesmo percebi que sobrescreveria o relato original do solicitante — má ideia, não um quick win) e **prazo/`due_date` continua 100% automático** (usuário confirmou a opção recomendada — `start_date`/`date_closed` também ficam de fora, alimentam SLA/tempo médio de atendimento, editar manualmente arriscaria corromper essas métricas sem ninguém notar).
    - **Edição inline de Status e Operador na Tabela** (`tasks-table.tsx`) — antes só dava pra mudar pelo modal "Gerenciar" (ou status via drag-and-drop no Quadro). Reaproveita exatamente a mesma rota (`POST /admin/tasks/:id`) — zero mudança de backend pra isso. `gestao-view.tsx`: a mutation que só existia pro drag-and-drop do Quadro (`dropMutation`) foi generalizada (`quickUpdateMutation`) pra aceitar qualquer `UpdatePayload`, reaproveitada pelos dois lugares.
    - **Setor como pill colorida na Tabela** — Tipo já era (desde a Fase 2 do painel de admin, 2026-08-07); Setor ainda era texto puro. Mesmo padrão, só estendido.
    - **Redesign da barra de filtros** (`filters-bar.tsx`) — mais compacta/densa, badge "N filtros" contando quantos dos 5 filtros estão ativos, busca com ícone inline. Mesma lógica de sempre por baixo (`set()`/`limpar()` intocados), só reskin — sem trocar componente/biblioteca.
    - **Dashboard com filtro de período** (`dashboard-view.tsx`, `d1GetMetrics`/`handleAdminMetrics` em `push-worker.js`) — usuário escolheu datas customizáveis (não só pré-sets) na pergunta de planejamento; implementado "de/até" livre + atalhos (Hoje/7 dias/30 dias/Tudo) que só preenchem os campos. Backend: `d1GetMetrics(env, {desde, ate})` filtra por `date_created` (que já existia na tabela — filtro aplicado em JS sobre o resultado, não na query, pelo volume atual não justificar) — resto da função (SLA, tempo médio por operador) intocado, só operando sobre o subconjunto. `handleAdminMetrics` valida `desde`/`ate` como número finito antes de repassar (400 se inválido). 6 testes novos (3 em `d1-layer.test.js`, 2 em `push-worker.test.js` HTTP + validação, mais o teste de contrato "sem parâmetro continua devolvendo tudo").
  - Verificado: 194 testes de backend (53+97+37+7), 0 falhando; `tsc -b`/`npm run build` limpos; Playwright (rede mockada) confirmando cada item — pill de Tipo/Setor visíveis, POST correto disparado ao mudar status inline (`{status: 'em atendimento'}`), badge "1 filtro" aparecendo, total do Dashboard mudando de 2→1 chamado ao clicar "Hoje" com respostas mockadas diferentes por período.

- **🐛 Logo ISV ilegível/com cor errada em todo o frontend React, corrigido (2026-08-14)** — usuário reportou print do "Novo Chamado" com a logo do header pequena/borrada e a interface "sem cor, estranha". Causa real, **não relacionada** à mudança de tamanho do dia anterior (essa só piorou um bug que já existia desde a Fase F1): `logo-isv.svg` é um lockup **largo** (~3,3:1 — ícone + texto "INSTITUTO SÃO VICENTE"), mas em todo lugar no React ele estava dentro de uma caixa **quadrada** (`h-X w-X`) — isso esmaga o texto até virar ilegível, em 4 lugares (`app-header.tsx`, `SolicitanteApp.tsx`, `app-sidebar.tsx`, e as telas de login `gate-screen.tsx`/`solicitante-setup.tsx`). Some com isso: `w-auto` no lugar de `w-X` nos headers (preserva a proporção real, ~130px de largura pra 40px de altura). Segundo achado, nos headers escuros: a classe `invert` sozinha (Tailwind `filter: invert(100%)`) **inverte as cores** do logo (fica com tons estranhos, roxo/laranja) em vez de virar silhueta branca — o header vanilla sempre usou `filter: brightness(0) invert(1)` (achata pra preto sólido primeiro, só depois inverte pra branco puro); corrigido adicionando `brightness-0` junto de `invert`. **Achado adicional, comparando com o vanilla**: as telas de login nunca deveriam ter usado `logo-isv.svg` — o vanilla (`index.html`/`admin.html`) sempre usou um ícone **quadrado** próprio ali (o mesmo glyph do ícone do PWA, `assets/icon.svg`, tela de monitor+relógio), nunca o logotipo largo; a versão React desviou disso na Fase F1 sem motivo documentado. Corrigido trocando pro ícone quadrado nas duas telas de login (`icon-isv.svg`, cópia de `assets/icon.svg` pra `frontend/src/assets/`, mesmo padrão de import de `logo-isv.svg`). Sidebar do admin (`app-sidebar.tsx`) ganhou um segundo `<img>` — mostra o logotipo largo (corrigido) quando expandida, troca pro ícone quadrado quando colapsada (não dá pra encaixar um lockup largo em 48px de rail), mesmo encaixe exato já documentado no código pro modo ícone. Verificado com `tsc -b`/build limpos e Playwright (rede mockada) nas 4 telas — login do solicitante, login do admin, header do solicitante logado, header+sidebar do admin (colapsada e expandida) — bounding box de cada logo conferido em pixel (larga = altura fixa/largura livre sem distorção; quadrada = 56×56 ou 32×32 exato, sem overflow). Nenhum teste de backend afetado (mudança 100% frontend).

- **🎨 Sidebar do admin oculta por padrão + logos ampliadas nos cabeçalhos (2026-08-13)** — pedido direto do usuário. `AdminShell` (`AdminApp.tsx`) passou a montar `<SidebarProvider defaultOpen={false}>` (era o padrão `true` do componente shadcn) — a sidebar do painel de admin agora abre **recolhida** (modo ícone) a cada carregamento; **achado ao investigar**: o `SidebarProvider` só *escreve* o cookie `sidebar_state` a cada toggle, nunca *lê* de volta no mount, então `defaultOpen` é de fato o que decide o estado a cada carregamento, sem risco de um cookie antigo sobrepor a mudança. Logo ISV nos 3 cabeçalhos persistentes (`app-header.tsx` do admin, header do `SolicitanteApp.tsx`, e o cabeçalho da própria sidebar em `app-sidebar.tsx`) subiu de `h-8 w-8` (32px) pra `h-10 w-10` (40px) — as telas de login (`gate-screen.tsx`/`solicitante-setup.tsx`, já `h-14 w-14`) não foram tocadas, não eram o alvo da reclamação. **Achado ao ampliar a logo da sidebar**: o cabeçalho da sidebar (`SidebarHeader`, `p-4`) tinha só 16px de sobra pro conteúdo quando colapsada (`--sidebar-width-icon` = 3rem/48px) — com a sidebar agora abrindo colapsada por padrão, uma logo maior (40px) estouraria esse espaço todo carregamento, não só quando alguém colapsasse manualmente (cenário raro até agora). Corrigido com classes `group-data-[collapsible=icon]:` — padding cai pra `p-2` e a logo volta a `h-8 w-8` só no estado colapsado, mantendo `h-10 w-10` expandida; encaixe exato confirmado via Playwright (`boundingBox` da logo == 32×32 dentro de um container de 48px de largura, sem overflow). Verificado com `tsc -b` limpo, build de produção, e Playwright (rede mockada) nos dois estados — colapsado no carregamento inicial e expandido depois de clicar no trigger, screenshot de cada um.
- **📦 Versionamento retroativo aplicado (2026-08-13): projeto saiu do zero-version.** 55 commits (Fases B2-M5 + alerta de admin) nunca tinham ganhado versão nenhuma — `frontend/package.json` travado em `0.0.0`, zero tags git. Aplicadas 30 tags anotadas retroativas (`v0.4.0` a `v1.1.0`), seguindo SemVer + Conventional Commits mecanicamente (`feat:`→MINOR, `fix:`→PATCH). **`v1.0.0` marca o fim do pré-produção (`MAJOR=0`), aplicada de propósito no commit que fecha a Fase M5** — o momento em que o sistema virou 100% autônomo (React + D1/R2, zero API externa). `frontend/package.json` (`version`) é o novo local vivo da versão declarada, substituindo o `<meta name="app-version">` do `index.html` vanilla (congelado em `0.3.5`, parte do artefato de rollback). Ver "Padrões de desenvolvimento" e "Decisões técnicas tomadas" pro detalhe completo.
- **🔔 Alerta de chamado novo no painel de admin (2026-08-13)** — com a ClickUp desligada (M1-M5), a TI perdeu o número flutuante que a interface dela dava quando um chamado chegava; pedido do usuário pra recriar isso dentro do próprio `admin.html`. Três mecanismos juntos: **push do navegador** (`POST /admin/subscribe`, novo — inscrição por navegador/dispositivo já que o admin não tem login por pessoa, só `ADMIN_SECRET` compartilhado; `handleCreateTask` avisa todo mundo inscrito depois de gravar o chamado no D1, best-effort, nunca derruba a criação), **badge flutuante** (sino + número no header, sempre visível em qualquer seção — contagem real de chamados "aberto", sobe/desce sozinha, sem estado "lido/não lido") e **som** (gerado por código via Web Audio API, sem arquivo de áudio nenhum pra versionar). A "caixa" que aparece quando chega um chamado novo (`NovoChamadoAlert`) tem descrição + botão grande "Aceitar chamado" (já muda pra "Em Atendimento" na hora, reaproveitando a mesma mutation do modal "Gerenciar"). Service Worker novo e **separado** só pro admin (`public/admin-sw.js`, JS puro, sem Workbox/manifest) — de propósito não reaproveita o `sw.js` do app de solicitantes, pra não contradizer "admin.html nunca deve ser PWA". **Achado real ao testar esta feature, sem relação direta com ela**: `d1TransitionStatus` tinha um bug — resumir de "pendente" resetava o prazo pra uma janela cheia nova (`now + padrão da prioridade`) em vez de só empurrar pelo tempo pausado, porque o bloco de "em atendimento" sobrescrevia o ajuste que o bloco de "saiu de pendente" já tinha calculado corretamente. Corrigido. Verificado em produção (rota nova respondendo, `admin-sw.js` publicado, `admin.html` continua sem manifest) + Playwright com rede mockada (badge/caixa funcionando, zero erro de console). 187 testes no total (53+93+34+7), 0 falhando. **🛡️ Auditoria do agente `revisor` no mesmo dia** — achou 1 crítico real (os dois Service Workers, `admin-sw.js` e `sw.js`, registravam no mesmo escopo — MPA com `admin.html`/`index.html` lado a lado no mesmo diretório — e um sobrescrevia o outro silenciosamente sempre que a TI abrisse os dois no mesmo navegador) e 3 avisos (o fix de SLA cobria só parte dos casos, validação incompleta de subscription em `POST /admin/subscribe`, `Set` de "chamados vistos" que nunca esquecia um id). Todos corrigidos no mesmo dia — ver "Decisões técnicas tomadas" pro detalhe completo de cada achado e correção.
- **🎉 Migração de saída da ClickUp CONCLUÍDA (2026-08-13, Fase M5): nenhuma rota de `push-worker.js` chama mais `api.clickup.com`.** Quinta e última fase do plano de 5 (M1-M5, mesmo dia — plano em `C:\Users\usuario\.claude\plans\cosmic-dazzling-pelican.md`). `handleGetTask` (`GET /api/tasks/:id`) — a última rota que ainda lia da ClickUp — corta pra D1 (`d1GetChamado` + `d1ListAnexos`); `handleSubscribe`/`d1TransitionStatus` trocam a chave de subscription de push de `u_<orderindex da ClickUp>` pra `usub_<nome>` direto (quem já tinha push ativo se resolve sozinho no próximo uso normal do app, mesmo padrão do incidente de troca de VAPID em 2026-08-10). Removidas as 3 rotas de migração de uso único que já tinham rodado em produção (`/admin/migrate-d1`, `/admin/migrate-solicitantes`, `/admin/migrate-anexos`) e `GET /api/field` (morta desde a M1). **A conta da ClickUp continua intacta, sem cancelar, como arquivo consultável manualmente** — decisão já tomada, não é um desligamento de dado, só de chamada de API. **Coordenação manual ainda pendente, fora do código**: desligar a automação de status configurada dentro da própria interface da ClickUp (ela chama `/webhook`, removida na M4, que já devolve 404 sem efeito nenhum — só fica como lixo de configuração até ser apagada lá). Verificado em produção: rotas removidas 404, `/api/solicitantes` continua devolvendo a lista real, `/admin/tasks/:id` continua exigindo `ADMIN_SECRET`. 180 testes no total (53+86+34+7), 0 falhando; build do frontend limpo, zero mudança (o contrato de resposta já era D1-native desde a B7). Ver "Decisões técnicas tomadas" e "D1 + R2 como backend" pro detalhe completo, incluindo um achado sobre o caminho de rollback do frontend (F5) ter ficado desatualizado por essa migração.
- **🚀 Migração de saída da ClickUp — Fase M4 concluída (2026-08-13, mesmo dia): status/solução/operador e automação de SLA viram síncronos no D1 — a fase mais delicada do plano.** Quarta de 5 fases. `handleAdminUpdateTask` (`POST /admin/tasks/:id`) parou de fazer PUT/POST na ClickUp — `status` chama `d1TransitionStatus` (Fase B5, dormente até agora) de forma síncrona, na mesma request (já cobre pausa/retomada de SLA em "pendente", `start_date`/`due_date` ao entrar em "em atendimento", `date_closed` ao "encerrado", e push — tudo sem depender de webhook nenhum); `solucao`/`assigneeId` viram `d1UpdateChamado` direto — `assigneeId` não precisa mais buscar quem já estava atribuído pra montar um diff `{add, rem}` contra a ClickUp, `d1UpdateChamado` com `assigneeIdsForSync` já substitui por completo a tabela `chamado_assignees` (mesmo `.batch()` atômico de sempre entre `assignee_id` e a junção). **Removidos `POST /webhook`, `handleWebhook` e `runStatusAutomation`** — fecha de quebra o item de segurança já documentado ("`POST /webhook` nunca teve autenticação"). **Coordenação manual pendente, fora do código**: a automação continua configurada dentro da própria interface da ClickUp e vai continuar chamando essa URL (que agora só devolve 404) até alguém desligá-la lá manualmente — sem efeito nenhum aqui, mas é lixo de configuração até ser removida (ver "Próximos passos"). Residual conhecido e adiado de propósito pra M5: `d1TransitionStatus` ainda resolve a inscrição de push via `getSolicitanteMaps` (única chamada restante a `api.clickup.com` nesta rota). Verificado em produção: `POST /webhook` agora devolve 404 (rota removida), `POST /admin/tasks/:id` sem `X-Admin-Secret` continua 403 (auth intacta). 190 testes no total (53+96+34+7) — bloco inteiro de testes reescrito sem mock de ClickUp, incluindo um teste de atomicidade que passou a refletir que o D1 é a escrita agora (falha vira erro real, não mais 200 silencioso de espelho best-effort). Ver "Decisões técnicas tomadas" pro detalhe completo.
- **🚀 Migração de saída da ClickUp — Fase M3 concluída (2026-08-13, mesmo dia): criar chamado vira D1-nativo.** Terceira de 5 fases. `handleCreateTask` deixou de criar a task na ClickUp primeiro — grava direto no D1 (`d1CreateChamado`, Fase B2, dormente até agora), `id` passa a ser `crypto.randomUUID()` (não mais o formato `86ajzr6x0` da ClickUp — mudança visível, sem impacto funcional conhecido). `solicitante: session.name` gravado direto, sem resolver orderindex nenhum — `getSolicitanteMaps` deixou de ser chamada por essa rota (ainda em uso por `handleGetTask`/`handleSubscribe`/`handleAdminMigrateD1`/`d1TransitionStatus`, fases M4/M5 removem o resto). Contrato HTTP de resposta não mudou pro frontend — `d1RowToTaskShape` (já usado por `GET /api/my-tasks`/`GET /admin/tasks`) devolve o mesmo shape de sempre; **zero mudança de código no frontend precisou** (confirmado com `tsc -b` limpo). Verificado em produção: deploy do Worker confirmado, `POST /api/tasks` sem sessão continua devolvendo 401 corretamente (endpoint vivo, auth intacta) — criação real end-to-end não foi testada com uma conta fake em produção de propósito (M1 fechou o autocadastro livre; criar uma conta de teste exigiria o `ADMIN_SECRET`, e usar uma conta de um solicitante real pra isso não é uma prática aceitável), então a garantia aqui vem dos 188 testes automatizados + revisão de código, mesmo padrão de cautela já usado na verificação da M2. 188 testes no total, sem teste novo de contagem (só os 3 que dependiam do mock antigo de "echo" da ClickUp foram reescritos pra verificar a resposta/D1 direto). Ver "Decisões técnicas tomadas" pro detalhe completo.
- **🚀 Migração de saída da ClickUp — Fase M2 concluída (2026-08-13): anexos saem da ClickUp, vão pro R2.** Segunda de 5 fases (plano em `C:\Users\usuario\.claude\plans\cosmic-dazzling-pelican.md`) — a que o próprio plano já identificava como maior risco técnico. `handleUploadAttachment` passou a gravar no R2 (`r2UploadAnexo`, Fase B4, dormente até agora) + `chamado_anexos` no D1, em vez de subir pra ClickUp; nova rota `GET /api/anexos/:id`, autenticada por sessão, confirma dono antes de servir (**melhoria de segurança real**: a URL antiga da ClickUp era pública/direta, sem sessão nenhuma pra abrir). `GET /tasks/:id` passou a montar `attachments` a partir do D1/R2, não mais do array bruto da ClickUp — teve que ir no MESMO deploy que o upload novo (achado de sequenciamento: se ficassem em deploys separados, o anexo recém-subido ficaria invisível no meio do caminho). **46 anexos reais migrados em 41 chamados, 0 erros**, via `POST /admin/migrate-anexos` — rota paginada desde o início (não é mais preciso descobrir estouro de subrequests na marra). **Achado real, corrigido na hora**: o teto de subrequests por invocação do Worker é bem menor do que os "~1000 no plano pago" documentados no dia anterior — lotes de 50 tasks estouravam consistentemente; lotes de 5 rodaram os 455 chamados reais (91 chamadas) sem nenhum erro. Frontend: anexo deixou de ser um `<img src>`/`<a href>` direto numa URL pública — `AnexoModal` agora busca o arquivo autenticado e monta um object URL antes de renderizar. Ver "Decisões técnicas tomadas" pro detalhe completo.
- **🚀 Migração de saída da ClickUp — Fase M1 concluída (2026-08-13): lista de solicitantes vira D1-nativa.** Primeira de 5 fases pra tirar a dependência da ClickUp (pedido do usuário, prazo fim de agosto/2026 — plano completo salvo em `C:\Users\usuario\.claude\plans\cosmic-dazzling-pelican.md`). Motivo de começar por aqui: o boot inteiro do app dos solicitantes travava (`state = "boot-error"`) se `GET /api/field` (ClickUp) falhasse — era o ponto de falha mais crítico do sistema inteiro e o único sem nenhuma solução pronta. Nova tabela `solicitantes` no D1 (`name` como chave, sem orderindex — diferente da ClickUp); `GET /api/solicitantes` substitui `GET /api/field` pro login e pro filtro do painel; **`handleRegister` passou a validar o nome contra essa tabela antes de deixar criar senha** — fecha uma lacuna real de segurança que existia desde sempre (antes, qualquer string virava conta, sem validação nenhuma server-side, só a UI restringia via dropdown). Nova tela "Usuários" no admin (`usuarios-view.tsx`) — TI adiciona/desativa solicitante direto pelo painel, sem tocar mais na ClickUp pra isso. **43 nomes reais migrados em produção com sucesso** (`POST /admin/migrate-solicitantes`, idempotente). Verificado ao vivo: dropdown de login carregando os 43 nomes reais do endpoint novo, zero erro de console. `getSolicitanteMaps`/ClickUp continuam em uso por enquanto (`handleCreateTask`/`handleGetTask`/`handleUploadAttachment`/`handleSubscribe`/`handleAdminMigrateD1`/`d1TransitionStatus`) — as próximas fases (M2-M5) removem essas dependências uma a uma. Ver "Decisões técnicas tomadas" pro detalhe completo, incluindo um achado de ordenação (`ORDER BY` do SQL não é sensível a locale, corrigido com `localeCompare` em JS).
- **🐛 Regressão ativa corrigida (2026-08-12, mesmo dia): nome do Solicitante errado no Kanban/Tabela do painel de admin.** Achada por acaso, pesquisando a migração de saída da ClickUp (usuário pediu "precisamos migrar até o fim do mês") — investigando como o frontend resolve "quem abriu o chamado" hoje. Causa: o corte de `GET /admin/tasks` pro D1 (B7 parte 2, fase 2, mais cedo no mesmo dia) nunca incluiu `SOLICITANTE_FIELD_ID` em `custom_fields` — o comentário original ("quem chama esta função já sabe de quem é a lista") só era verdade enquanto só `GET /api/my-tasks` usava `d1RowToTaskShape` (1 lista por pessoa); deixou de valer quando `GET /admin/tasks` passou a usar a mesma função pra uma lista com chamados de gente diferente em cada linha. Resultado real: o frontend fazia `Number(getCF(task, SOLICITANTE_FIELD_ID))` → `Number(null)` → `0` → mostrava quem tivesse `orderindex 0` como solicitante de **todo** chamado — não um "—", um nome errado. Confirmado via grep no JSON de produção já buscado nesta sessão (o UUID do campo não aparecia em nenhum dos 454 chamados). **Corrigido expondo `solicitante` como campo próprio** (string, nome já resolvido — D1 nunca guarda orderindex) em `d1RowToTaskShape`, em vez de tentar reconstruir via `custom_fields`/orderindex (que exigiria bater na ClickUp de novo). Frontend (`kanban-board.tsx`/`tasks-table.tsx`) passou a ler `task.solicitante` direto; a prop `solicitanteIdxToName` (agora morta nesses 2 componentes) foi removida. **Verificado ao vivo em produção:** 36 nomes distintos de solicitante aparecendo corretamente nos 454 chamados (antes do fix, todos mostrariam o mesmo nome errado); os 269 chamados antigos sem SOLICITANTE (ver Fase B3) corretamente mostram "—", não um nome inventado.
- **🎨 Regressão visual da F5 corrigida (2026-08-12): tratamento de marca (gradiente/sombra) restaurado no frontend React.** Achado pelo usuário direto em produção (print da tela "Novo Chamado" parecendo "morta"/genérica) — investigado com Playwright (live vs `index.html` vanilla local, lado a lado): CSS carregava certo, zero erro de console, hex das cores batia com `css/style.css`, mas a Fase F1 só tinha portado os *tokens* de cor, não os *efeitos* (fundo em gradiente diagonal no login/gate, header/sidebar em gradiente, sombra grande no card `0 24px 64px`, botão primário em gradiente com leve "lift" no hover) — sem isso a paleta certa renderizava como shadcn genérico chapado. Restaurado em `frontend/src/index.css` (`.bg-brand-gradient`/`.bg-sidebar-gradient`/`.shadow-brand-card`, valores fixos — o app nunca teve modo escuro) e aplicado em `solicitante-setup.tsx`/`gate-screen.tsx`/`app-header.tsx`/`SolicitanteApp.tsx`/`app-sidebar.tsx`/`button.tsx`. Achado à parte: o `<Sidebar>` do shadcn não expõe a div interna que pinta o fundo (`data-slot="sidebar-inner"`) pro `className` do componente — precisou de um selector CSS descendente em vez de mexer no primitivo compartilhado. Verificado com build de produção real + Playwright (preview local com rede mockada) e depois ao vivo em produção nas duas telas de login (solicitante e admin) — confirmado batendo com o app antigo.
- **🚀 B7 parte 2 concluída — fase 1 e fase 2 (2026-08-12): D1 suporta múltiplos operadores por chamado, e `GET /admin/tasks`/`GET /admin/metrics` já leem daí.** Nova tabela `chamado_assignees` (N-pra-N, junction table — ver `d1/schema.sql`), `mapClickUpTaskToD1` captura o array completo de `assignees[]` (não só o primeiro), `d1SetAssignees` sincroniza a tabela em toda criação/atualização/migração de chamado. **Backfill dos 454 chamados já existentes rodado em produção com sucesso, 0 erros** — motivo real de existir uma fase 2 pra isso: o schema atual (`chamados.assignee_id`) só guardava 1 operador, então mesmo os 453/453 migrados na parte 1 tinham perdido o 2º+ operador de quem tinha mais de um atribuído. Achado real de produção nesse backfill: a primeira tentativa estourou o teto de subrequests por invocação do Worker (erro da Cloudflare "Too many API requests by single Worker invocation", ~1000 no plano pago) na volta de ~332/453 chamados, porque a versão original fazia 1 DELETE + N INSERT sequenciais por chamado; corrigido usando `env.CHAMADOS_DB.batch([...])` (manda todos os statements num round-trip só), reduzindo pra 2 subrequests/linha — reprocessado com sucesso total depois do fix. **Fase 2 (mesmo dia, na sequência):** `GET /admin/tasks`/`GET /admin/metrics` deixaram de paginar a ClickUp inteira a cada chamada e passaram a ler do D1 (`d1ListChamados`/`d1GetMetrics`) — `filters.withAssignees` (opt-in, só as rotas de admin pedem) anexa o array completo de assignees por chamado, preservando o aviso de "múltiplos operadores" do painel. **Achado real (2º incidente do mesmo dia, corrigido em minutos):** o primeiro deploy da fase 2 quebrou `/admin/tasks`/`/admin/metrics` em produção com **500 puro** (`error code: 1101`) sempre que o resultado batia em centenas de linhas (ex.: `/admin/metrics` sem filtro, `/admin/tasks?status=encerrado`) — `d1GetAssigneesMap` mandava todos os ids num `IN (...)` só, estourando o limite de variáveis bindáveis por statement do SQLite/D1; corrigido dividindo em pedaços de 50 ids por query. Verificado ao vivo em produção depois do fix: `/admin/tasks`/`/admin/metrics` voltaram a 200, total bate com os 454 reais, e o chamado real `86af8erzt` ("Atolamento de papel") aparece com os **2 operadores reais** (`[112089386, 200498355]`) em `/admin/tasks` — o cenário exato que motivou toda a B7 parte 2. Ver "Decisões técnicas tomadas" pro detalhe completo.
- **🚀 Fase B7 concluída — parte 1 (2026-08-12): D1 em produção de verdade, escrita em espelho + leitura de `GET /api/my-tasks` já vem do D1.** Ver "Decisões técnicas tomadas" pro detalhe completo (escopo deliberadamente parcial — `GET /admin/tasks`, `GET /admin/metrics` e `GET /tasks/:id` continuam na ClickUp de propósito, por razões concretas documentadas lá). **453 de 453 chamados reais migrados pro D1** — os 12 que faltavam (sem TIPO/SETOR na ClickUp) foram fechados no mesmo dia: `tipo`/`setor` deixaram de ser `NOT NULL` no schema (migração de schema própria, recria a tabela preservando os dados) e passam a migrar como `NULL` em vez de erro.
- **🚀 Fase F5 concluída (2026-08-12): o frontend em produção agora é o build React** (`frontend/`), publicado via `.github/workflows/deploy-frontend.yml` no GitHub Pages. Os arquivos vanilla (`index.html`/`admin.html`/`js/`/`css/`/`sw.js`/`manifest.json` na raiz) foram **retirados da produção** (continuam no repo como caminho de rollback, ver "Estrutura de arquivos" e "Deploy do Frontend" — não são mais o que roda pro usuário). `<meta name="app-version">`/footer do `index.html` antigo (versionamento `v0.3.x`) não se aplica mais — o frontend novo não tem esquema de versão próprio ainda declarado.
- **PWA funcional** integrado ao ClickUp como backend (lista `901324490220`), sem banco próprio.
- **O que funciona hoje:**
  - Abertura de chamado com tipo/setor/descrição e **anexo opcional** (limite 10 MB; suporta colar print via Ctrl+V).
  - **Prioridade automática** pelo tipo do chamado (`CATEGORIA_PRIORIDADE`); prioridade "Baixa" não existe.
  - Acompanhamento "Meus Chamados"/"Meu Histórico" filtrando tasks pelo campo `SOLICITANTE`, com SLA informativo e indicação de atraso.
  - **SLA pausa em "Pendente"**; solução aplicada lida de campo customizado dedicado e destacada em chamados encerrados.
  - Visualização de anexo em modal central; WhatsApp roteado pelo operador atribuído.
  - **Notificações push** via Cloudflare Worker (`chamados-ti-push.tecnologiadainformacao-isv.workers.dev`), acionadas por automação do ClickUp na mudança de status.
  - **Login com senha** (pedido da diretoria) — identidade decidida pelo Worker via sessão, ninguém vê chamado de outra pessoa mesmo selecionando o nome dela. Ver seção "Autenticação".
  - **Lista de solicitantes buscada em runtime da ClickUp** (`loadSolicitantes()`), com tela de boot/loading antes do login; sem mais array fixo pra manter sincronizado a cada colaborador novo.
  - **Painel de admin** (React, `frontend/` → `admin.html` em produção desde a F5) — deixou de ser só leitura: além de consumir `GET /admin/tasks`/`GET /admin/metrics`, agora tem `POST /admin/tasks/:id` pra mudar status, escrever solução e atribuir operador (botão "Gerenciar" por linha) — é onde a TI trabalha, sem precisar mais abrir a ClickUp. Ver "Painel de admin". Segredo de admin nunca fica no código, só no `localStorage` de quem loga na tela de gate.
- **Tratamento de erros amigável** e suporte offline básico implementados (v0.2.0) — obs: o boot atual (busca da lista de solicitantes) depende de rede mesmo pra quem já tinha configurado o app; ver "Próximos passos".
- **Testes unitários** em `tests/*.test.js` (`app.test.js`, `push-worker.test.js`, `d1-layer.test.js`, `r2-layer.test.js` — 188 testes no total, ver "Decisões técnicas tomadas" pra evolução por fase) — sem dependências de runtime: `node vm`/`fetch`/`node:sqlite`/`assert`, todos nativos do Node. Rodar tudo de uma vez com `npm test` (ou `node tests/run-all.js`); `package.json` existe só por causa desse atalho, não declara nenhuma dependência.
- **Deploy do Worker automatizado** (2026-08-10) — `push-worker.js` deixou de ser colado manualmente no dashboard da Cloudflare; agora publica via Wrangler + GitHub Actions a cada push na `main`. Ver "Deploy do Worker".
- **🔥 Incidente de produção (2026-08-10): "Abrir Chamado" não funcionava pra ninguém.** Detectado no dia do rollout pra todo o escritório. Três causas, achadas e corrigidas na sequência (a 3ª era a raiz real):
  1. `sw.js` desviava do cache só pra `api.clickup.com`, domínio que o app não chama mais direto (fala com o Worker via `workers.dev` há muito tempo) — qualquer chamada de API, principalmente POST, caía num `caches.match()` que quebra com "Failed to fetch" ao tentar reler o corpo da requisição. Corrigido: bypass do cache pra qualquer chamada de API (não só `api.clickup.com`) e pra qualquer método != GET.
  2. Efeito colateral do próprio deploy automatizado: os 2 primeiros deploys via Wrangler apagaram `VAPID_PRIVATE_JWK`/`VAPID_PUBLIC_KEY` (não eram `secret_text`, não estavam declaradas no `wrangler.toml`, e não existia `keep_vars`). Corrigido com `keep_vars = true` (ver "Deploy do Worker") + par VAPID novo gerado (o antigo não tinha backup, foi perdido de vez) — quem já tinha push ativado precisou reabrir o app uma vez pra reinscrever.
  3. **Causa raiz real:** `handleCreateTask` grava um throttle anti-duplo-clique no KV com `expirationTtl: 10` — o Cloudflare KV exige mínimo de 60s, então esse `PUT` sempre falhava com 400, sem tratamento, derrubando a função antes de criar o chamado na ClickUp. Isso **sempre esteve quebrado desde que o throttle foi escrito**; só ficou visível agora porque o pipeline novo publicou, pela primeira vez de verdade, a versão atual do arquivo (deploys manuais anteriores estavam desatualizados e nunca chegaram a incluir essa linha em produção). Corrigido: TTL 10 → 60. O mock de KV nos testes (`makeMockKV`) ignorava `expirationTtl` por completo, deixando esse bug invisível em 106 testes que sempre passaram — reforçado pra validar isso de verdade (ver `tests/push-worker.test.js`).
  Diagnosticado com `wrangler tail` (log em tempo real do Worker em produção) — foi o que revelou a exceção real por trás do que os navegadores reportavam como bloqueio de CORS.

## Decisões técnicas tomadas

- **🚀 Migração de saída da ClickUp — Fase M1 concluída (2026-08-13): lista de solicitantes vira D1-nativa.** Usuário pediu pra parar de depender da ClickUp, com prazo até o fim de agosto/2026. Plano completo em 5 fases (M1-M5) desenhado e aprovado em modo de planejamento, salvo em `C:\Users\usuario\.claude\plans\cosmic-dazzling-pelican.md` — decisões já confirmadas com o usuário: lista de solicitantes vira tabela no D1 + tela de gestão (não autocadastro livre, pra não reintroduzir o bug de 2026-07 que fez a lista fixa antiga ser abandonada), anexos antigos migram todos pro R2 antes do corte, e a conta da ClickUp fica desligada mas intacta como arquivo no final (mesmo padrão de rollback já usado na F5).
  - **Schema**: nova tabela `solicitantes (name TEXT PRIMARY KEY, ativo INTEGER, created_at INTEGER)` em `d1/schema.sql` — `name` é a chave, sem indireção de orderindex nenhuma (diferente da ClickUp); desativar em vez de apagar, pra chamados antigos continuarem referenciando o nome no histórico. Tabela criada em produção via rota de uso único `POST /admin/migrate-schema-solicitantes` (mesmo padrão de `POST /admin/migrate-schema-chamado-assignees`) — **achado repetido nesta sessão**: mudança de código no Worker não aplica mudança de schema no D1 remoto sozinha, sempre precisa de uma rota dedicada rodada manualmente uma vez.
  - **`GET /api/solicitantes`** substitui `GET /api/field` pro app de solicitantes (tela de login) e pro filtro do painel de admin — mesma auth pública (`X-App-Secret`), lê só os ativos. `getSolicitanteMaps`/`GET /api/field` continuam existindo e em uso por enquanto — `handleCreateTask`, `handleGetTask`, `handleUploadAttachment`, `handleSubscribe`, `handleAdminMigrateD1` e `d1TransitionStatus` ainda dependem da ClickUp pra resolver nome↔orderindex; as fases M2-M5 removem isso peça por peça.
  - **`handleRegister` passou a validar** que o nome existe e está ativo em `solicitantes` antes de deixar criar senha — **fecha uma lacuna real de segurança que sempre existiu**: antes, a única "trava" contra registrar qualquer string como conta era o dropdown da UI (que confia no cliente); o servidor nunca validou nada. Achado ao construir esta fase, não relatado antes.
  - **Nova tela "Usuários"** no admin (`frontend/src/components/usuarios-view.tsx`, aba nova na sidebar ao lado de Gestão/Dashboard) — lista todos (ativos e inativos, pra poder reativar), formulário de adicionar, botão ativar/desativar por linha. `GET/POST /admin/solicitantes` (listar/criar) e `POST /admin/solicitantes/:nome/ativo` (toggle) — `ADMIN_SECRET`, mesmo padrão de toda rota `/admin/*`. Sem componente `Table`/`Switch` do shadcn instalado no projeto ainda — reaproveitou `<table>` cru (mesmo padrão de `tasks-table.tsx`) e `Button` pro toggle, em vez de instalar primitivos novos só pra isso.
  - **`POST /admin/migrate-solicitantes`** (uso único, idempotente) copia a lista que hoje vive na ClickUp pra tabela nova — reaproveita `getSolicitanteMaps`, mesmo padrão de todas as outras migrações desta sessão. **43 nomes reais migrados em produção, 0 erros, confirmado idempotente** (rodar de novo: `migrated:0, skipped:43`).
  - 🛡️ **Achado de ordenação, corrigido — mas com uma ressalva honesta**: `ORDER BY name` no SQL do SQLite/D1 não é sensível a locale (compara por byte cru, não por colação), então nomes acentuados podiam ficar fora de ordem — a versão ClickUp-based sempre ordenou em JS com `localeCompare(nome, 'pt-BR')`; `d1ListSolicitantes` passou a fazer o mesmo depois de buscar. **Ressalva**: testado localmente (Node) esse fix mudava a ordem relativa entre "Márcio"/"Mariana"; verificado ao vivo em produção depois do deploy, a ordem real dos 43 nomes ficou **idêntica** à de antes do fix — o runtime real do Cloudflare Workers (`workerd`) parece produzir uma colação pt-BR ligeiramente diferente da do Node.js local pra esse caso específico. O fix não piorou nada e é logicamente mais correto (mesmo mecanismo que o resto do projeto sempre usou), mas não houve uma regressão visível de fato corrigida na prática — registrado aqui pra não reabrir a investigação achando que ainda tem bug se alguém notar "Márcio" nessa posição específica de novo.
  - 179 testes no total (53+85+34+7) — 13 novos cobrindo CRUD de solicitantes, validação de registro, migração idempotente e ordenação.
- **🚀 Migração de saída da ClickUp — Fase M2 concluída (2026-08-13, mesmo dia): anexos saem da ClickUp.** Maior risco técnico do plano inteiro (volume desconhecido de anexos antigos, teto de subrequests).
  - **Schema**: nova tabela `chamado_anexos (id TEXT PRIMARY KEY, chamado_id, r2_key, filename, content_type, size, created_at)` — `id` é gerado pela aplicação (não a `r2_key` bruta, não expõe o formato interno da key do bucket pro cliente). Sem `FOREIGN KEY`, mesmo motivo de `chamado_assignees`. Criada em produção via `POST /admin/migrate-schema-anexos` (mesmo padrão de sempre).
  - **`handleUploadAttachment`** troca o `POST` pra ClickUp por `r2UploadAnexo` (Fase B4, dormente até agora) + `INSERT` em `chamado_anexos`. Checagem de dono do chamado passou a usar `d1GetChamado` (D1) em vez de bater na ClickUp de novo — o chamado já está espelhado lá desde a criação (Fase B7), não tem motivo pra pagar esse round-trip de novo. **Achado ao construir esta fase**: o limite de 10MB por anexo sempre existiu só no cliente — nada no servidor jamais impediu upload maior. Agora `MAX_ANEXO_BYTES` também é aplicado no Worker.
  - **`GET /api/anexos/:id`** (nova rota) — autenticada por sessão, confirma dono antes de servir o arquivo do R2 (`r2GetAnexo`). **Melhoria de segurança real em relação ao comportamento anterior**: a URL da ClickUp sempre foi pública/direta — qualquer um com o link (visível no HTML/JS do app) conseguia abrir o anexo sem sessão nenhuma. A rota nova exige login de dono.
  - **`GET /tasks/:id` (`handleGetTask`) passou a montar `attachments` a partir de `chamado_anexos`** (D1), não mais do array bruto que a ClickUp devolvia. **Achado de sequenciamento, decidido antes de deployar** (diferente de toda migração anterior desta sessão, que sempre separou "escrita" de "corte de leitura" em 2 deploys): aqui NÃO dava pra separar — se o upload já fosse pro R2 mas esta rota continuasse lendo `attachments` da ClickUp, qualquer anexo subido depois do deploy do upload ficaria **invisível** até o segundo deploy acontecer (não é o mesmo padrão de "ClickUp continua certa, D1 só ainda não é lido" das fases B7 — aqui a ClickUp já não tem mais o dado desde o primeiro deploy). Os dois foram no mesmo deploy, junto com o frontend (`AnexoModal`) — as três pontas são interdependentes.
  - **`POST /admin/migrate-anexos`** (uso único) — pra cada chamado com anexo na ClickUp, baixa e sobe pro R2. **PAGINADA desde o design inicial** (aceita `{offset, limit}`), decisão tomada de propósito por causa da lição da B7 parte 2/fase 1 (estouro de subrequests no backfill de `chamado_assignees`, 2026-08-12) — não repetir o erro de descobrir o teto na marra. Idempotente (confere `chamado_id`+`filename` antes de baixar de novo).
    - 🛡️ **Achado real rodando em produção, MESMO com a rota já paginada**: `limit:50` (padrão inicial) ainda estourava o teto de subrequests por invocação consistentemente, com o erro real da Cloudflare `"Too many subrequests by single Worker invocation"` — pior, uma vez estourado, TODO o resto do lote falhava também (até o `GET /task/:id` que só checa se a task tem anexo, fazendo tasks SEM anexo nenhum aparecerem como "erro"). **O teto real de subrequests por invocação neste Worker é bem menor do que os "~1000 no plano pago" documentados no dia anterior pro backfill de `chamado_assignees`** (mesma classe de erro, limites empíricos diferentes — talvez o plano real seja outro, ou R2/D1 contem diferente pra esse orçamento; não totalmente esclarecido, só contornado). Corrigido reduzindo o padrão de `limit` pra 5 — rodou os 455 chamados reais (91 chamadas) com **zero erros**. **Lição pro projeto**: não assumir nenhum número de subrequests como "seguro" sem testar empiricamente com lotes pequenos primeiro, nem mesmo um número já visto funcionar numa migração anterior — o orçamento real parece variar por tipo de operação.
    - **Resultado final em produção**: 46 anexos reais migrados em 41 chamados, 0 erros, confirmado idempotente.
  - **Frontend**: `anexo.url` deixou de ser um link público/direto (funcionava em `<img src>`/`<a href>` sem header nenhum) e virou uma rota autenticada — não dá mais pra usar direto. `fetchAnexoBlob` (`app-api.ts`) busca o arquivo com sessão; `AnexoModal` monta um `object URL` (`URL.createObjectURL`) antes de renderizar, com estado de carregando/erro novo; `ticket-card.tsx` passa `sessionToken` pra baixo.
  - 188 testes no total (53+94+34+7) — mock de R2 (`makeMockR2`) adicionado a `tests/push-worker.test.js` (reaproveitando o padrão de `tests/r2-layer.test.js`, agora também aceitando `ReadableStream`, já que upload/migração passam stream direto pro R2 real). 18 testes novos cobrindo upload, limite de tamanho, `GET /api/anexos/:id` (dono/não-dono/inexistente), migração com/sem anexo, idempotência, paginação, e `GET /tasks/:id` devolvendo os anexos certos pela rota nova.
- **🚀 Migração de saída da ClickUp — Fase M3 concluída (2026-08-13, mesmo dia): criar chamado vira D1-nativo.** Terceira de 5 fases — a mais simples até agora, porque M1 (solicitantes) e M2 (anexos) já tinham removido as duas dependências reais que `handleCreateTask` tinha na ClickUp além de criar a própria task.
  - **`handleCreateTask` reescrito**: em vez de `POST` na ClickUp e devolver o JSON bruto dela, chama `d1CreateChamado(env, {...})` (Fase B2, escrita/testada em 2026-08-11, nunca usada em produção até agora) e devolve `jsonRes(d1RowToTaskShape(row))` — a mesma função que já monta a resposta de `GET /api/my-tasks`/`GET /admin/tasks`, então o formato que o frontend recebe não muda de forma prática. Prioridade/`due_date` continuam **calculados no servidor** a partir do TIPO (`CATEGORIA_PRIORIDADE`/`PRIORITY_SLA_MS`) — regra de negócio intocada, só a gravação final que muda de destino.
  - **`id` muda de formato**: era o ID da ClickUp (ex.: `86ajzr6x0`), vira `crypto.randomUUID()` (mesmo padrão que `d1CreateChamado` já usava desde a B2, só nunca tinha sido exercitado em produção). Registrado aqui de propósito como mudança visível — não deveria ter impacto funcional (nada no app depende do formato do id), mas é o tipo de coisa que vale saber se aparecer estranho num relatório/print.
  - **`solicitante` gravado direto de `session.name`** — `getSolicitanteMaps`/resolução de orderindex, que `handleCreateTask` sempre precisou pra montar o campo customizado `SOLICITANTE` da ClickUp, deixou de ser chamada por essa rota (D1 nunca guardou orderindex, só o nome puro, ver Fase M1). Ainda em uso por `handleGetTask`, `handleSubscribe`, `handleAdminMigrateD1` e `d1TransitionStatus` — cada um cai nas fases M4/M5.
  - **Zero mudança de frontend** — confirmado rodando `npx tsc -b` limpo em `frontend/` depois da mudança de backend. `WaModal`/`waNumberForTask`/`novo-chamado-form.tsx` só leem `task.name`/`task.assignees[0].id`/`task.id`, todos presentes no shape de sempre.
  - **Testes**: os 3 testes que dependiam de `lastCreatePayload` (variável que o mock global de `fetch` populava só quando `handleCreateTask` ainda fazia `POST` pra ClickUp) foram reescritos pra verificar a resposta JSON e/ou `d1GetChamado(env, created.id)` diretamente — inclui os dois testes de segurança que continuam valendo (forjar SOLICITANTE de outra pessoa é ignorado; forjar prioridade/due_date é ignorado, servidor recalcula pelo TIPO). O branch morto do mock (`if (opts?.method === 'POST') {...}` dentro do handler de `/list/:id/task`, que simulava a ClickUp ecoando de volta a task criada) foi removido — não é mais alcançado por nenhum código de produção. 188 testes no total (53+94+34+7), 0 falhando.
  - 🛡️ **Verificação em produção deliberadamente limitada**: confirmado o deploy (workflow do Worker verde) e que `POST /api/tasks` sem sessão continua devolvendo 401 (prova que o endpoint está no ar e a checagem de auth não regrediu). Não foi criado um chamado real de teste em produção — M1 fechou o autocadastro livre (`handleRegister` agora exige o nome já existir e estar ativo em `solicitantes`), então um teste de escrita de verdade exigiria ou o `ADMIN_SECRET` (pra cadastrar um solicitante de teste) ou usar a conta de um solicitante real — nenhuma das duas foi feita, mesmo padrão de cautela já seguido na verificação da M2 (evitar usar identidade real/fake em produção só pra testar). A garantia de correção aqui vem dos 188 testes automatizados (que exercitam `d1CreateChamado` de verdade, não uma reimplementação paralela) + revisão de código, não de uma chamada real ao vivo.
- **🚀 Migração de saída da ClickUp — Fase M4 concluída (2026-08-13, mesmo dia): status/solução/operador e automação de SLA viram síncronos no D1.** Quarta de 5 fases — a mais delicada segundo o próprio plano, porque troca a automação de status inteira de "ClickUp decide, um webhook avisa depois" pra "D1 decide na hora, na mesma request".
  - **`handleAdminUpdateTask` reescrito**: cada sub-mutação deixou de ser um PUT/POST na ClickUp. `status` chama `d1TransitionStatus(env, taskId, body.status)` (Fase B5, escrita/testada em 2026-08-11, nunca usada em produção até agora) — já embute o que antes vinha de `runStatusAutomation` via webhook: pausa/retomada de SLA em "pendente" (usa o mesmo padrão de chave `d1_pending_start_<id>` no KV, paralelo ao `pending_start_<id>` que a versão antiga usava), `start_date`/`due_date` ao entrar em "em atendimento" (pelo padrão de tempo por prioridade, `DEFAULT_TIME_ESTIMATE_MS_BY_PRIORITY` — sem suporte a `time_estimate` manual por task, lacuna já conhecida e aceita desde que a função foi escrita), `date_closed` ao "encerrado", e push (via `getSolicitanteMaps`+`sendWebPush`, ver achado abaixo) — tudo síncrono, sem depender de nenhuma automação configurada na ClickUp. `solucao`/`assigneeId` viram `d1UpdateChamado` direto; `assigneeId` não precisa mais buscar quem já estava atribuído pra montar um diff `{add, rem}` (isso só existia por causa da API da ClickUp não ter "set assignee") — `d1UpdateChamado` com `assigneeIdsForSync` já SUBSTITUI por completo a tabela `chamado_assignees`, no mesmo `.batch()` que atualiza `assignee_id` (mesma atomicidade que já existia pro espelho, agora valendo pra escrita real).
  - **Sem transação real entre as 3 sub-mutações** (status/solução/operador) — isso não mudou, é o mesmo limite aceito desde 2026-08-07 (cada uma é sua própria escrita no D1 agora, antes era sua própria chamada à ClickUp); uma falha no meio continua reportando em `updated` o que já foi aplicado antes de parar, mesma proteção do revisor contra mascarar estado parcial.
  - **Removidos `POST /webhook`, `handleWebhook` e `runStatusAutomation`** — não sobrou nada reagindo ao evento `taskStatusUpdated` da ClickUp. **Fecha de quebra um item de segurança que já estava documentado no CLAUDE.md**: `POST /webhook` nunca teve autenticação nenhuma — qualquer um que soubesse a URL do Worker (hardcoded em `app.js`, pública) e um `task_id` real podia forjar uma mudança de status.
  - 🛡️ **Coordenação manual pendente, fora do código** (nota do próprio plano, cumprida): a automação correspondente continua configurada dentro da interface da ClickUp e vai continuar tentando chamar essa URL (que agora só devolve 404) até alguém desligá-la/apagá-la lá manualmente — sem efeito nenhum aqui (a chamada da ClickUp simplesmente falha em silêncio do lado dela), mas fica como lixo de configuração até ser removida. Decidido deixar essa automação ligada até confirmar M4 estável em produção por alguns dias, pra não perder notificação no meio da transição — mesmo raciocínio já registrado no plano.
  - 🛡️ **Residual conhecido, documentado, adiado de propósito pra M5**: `d1TransitionStatus` ainda resolve a inscrição de push via `getSolicitanteMaps` (nome→orderindex da ClickUp) — é a única chamada restante a `api.clickup.com` que sobra em `POST /admin/tasks/:id`. Não foi resolvido nesta fase porque o plano aprovado já reserva essa limpeza pra M5 ("Remove os 15 fetch pra api.clickup.com que sobrarem"); trocar o esquema de chave de subscription (`u_<orderindex>` pra algo baseado em nome) exigiria também mexer em `handleSubscribe`, fora do escopo desta fase.
  - **Verificado em produção**: `POST /webhook` agora devolve 404 (rota removida — antes respondia, mesmo sem autenticação nenhuma); `POST /admin/tasks/:id` sem `X-Admin-Secret` continua 403 (auth não regrediu). Sem mutação real de teste em produção, mesmo padrão de cautela das fases anteriores.
  - **Testes**: bloco inteiro de "POST /admin/tasks/:id" reescrito sem nenhum mock de ClickUp — `criarChamadoTeste()` (helper novo) semeia fixture via `d1CreateChamado`, verificação direto contra `d1GetChamado`/`chamado_assignees`. Testes novos: 404 pra chamado inexistente, `start_date`/`due_date` corretos por prioridade ao entrar em "em atendimento", `date_closed` ao "encerrado", pausa/retomada de SLA em "pendente" (devolve o due_date empurrado certo), `assigneeId:null` limpando a tabela de junção. **Teste de atomicidade reescrito** pra refletir a mudança de semântica: antes, uma falha no `.batch()` do espelho ainda respondia 200 (a mutação "de verdade" já tinha ido pra ClickUp); agora o D1 É a escrita, então a mesma falha vira erro real (400) — a garantia que sempre importou de verdade (achado do revisor 2026-08-12: `assignee_id` e `chamado_assignees` nunca divergirem entre si) continua coberta, porque as duas seguem no mesmo `.batch()`. 190 testes no total (53+96+34+7), 0 falhando.
- **🎉 Migração de saída da ClickUp — Fase M5 concluída (2026-08-13, mesmo dia): leitura final via D1 e integração desligada — última fase do plano.**
  - **`handleGetTask` (`GET /api/tasks/:id`) cortado pra D1** — era a última rota que ainda lia da ClickUp (a M2 já tinha tirado só os anexos de lá). Agora usa `d1GetChamado` + `d1ListAnexos` + `d1RowToTaskShape` (mesma função que `GET /api/my-tasks`/`GET /admin/tasks` já usavam), checagem de dono vira `chamado.solicitante === session.name` direto — sem resolver orderindex nenhum.
  - **Chave de subscription de push trocou de `u_<orderindex da ClickUp>` pra `usub_<nome>`** em `handleSubscribe` e `d1TransitionStatus` — a única razão pra manter `getSolicitanteMaps` viva depois da M4 era resolver esse índice; virando baseada em nome direto, a função inteira (e a chamada `GET /list/:id/field` que ela fazia) deixa de ser necessária. **Efeito colateral aceito, auto-resolvível**: quem já tinha push ativo sob a chave antiga fica sem notificação até o navegador re-chamar `POST /subscribe` — o que já acontece sozinho toda vez que o app carrega com a permissão de notificação concedida (`usePushNotifications`, `Notification.permission === "granted"` → `subscribeToPush()` de novo), então basta reabrir o app uma vez. Mesmo padrão de disrupção auto-resolvível já aceito no incidente de troca de par VAPID (2026-08-10).
  - **Removidas as 3 rotas de migração de uso único que já tinham rodado em produção**: `POST /admin/migrate-d1` (453/453 migrados, Fase B7), `POST /admin/migrate-solicitantes` (43 nomes, Fase M1), `POST /admin/migrate-anexos` (46 anexos/41 chamados, Fase M2) — e `GET /api/field` (morta desde a M1, sem consumidor no frontend). Funções que só existiam pra alimentar essas rotas também removidas: `fetchAllTasks`, `getSolicitanteMaps`, `mapClickUpTaskToD1`, `d1MigrateChamado`, `cfValue`, `handleGetField`, e `passthrough` (ficou sem nenhum chamador depois de `handleGetField` sair). Rotas de migração de **schema** (`migrate-schema-*`) continuam — são D1-only, nunca dependeram da ClickUp. `LIST_ID`/`EMAIL_FIELD_ID`/`SOLICITANTE_FIELD_ID` ficam como constantes vestigiais (só documentação de qual lista/campo da ClickUp arquivada essas rotas costumavam ler).
  - **Nenhuma rota chama mais `fetch()` pra `api.clickup.com`** — confirmado por grep no arquivo inteiro depois das mudanças. `env.CLICKUP_API_KEY` deixou de ser lida por qualquer código — **decisão deliberada de deixar o secret configurado por enquanto** (não bloqueia nada, remove-lo é opcional/futuro via `wrangler secret delete`, quando quiserem reduzir superfície). A conta da ClickUp em si **não foi cancelada** — decisão já tomada desde o início do plano, fica intacta como arquivo consultável manualmente.
  - 🛡️ **Coordenação manual ainda pendente, fora do código** (mesma nota da M4, ainda não resolvida): a automação de status configurada dentro da própria interface da ClickUp continua tentando chamar `/webhook` (removida na M4, devolve 404) — sem efeito nenhum aqui, só fica como configuração órfã até alguém apagá-la manualmente na ClickUp. Não é bloqueante pra considerar a migração concluída.
  - 🛡️ **Achado sobre o caminho de rollback do frontend (F5), descoberto ao revisar a documentação nesta fase**: o procedimento de rollback já documentado ("Deploy do Frontend" → "Como reverter") assumia implicitamente que o Worker continuava ClickUp-nativo por baixo dos arquivos vanilla (`js/app.js`/`js/admin.js`) — deixou de ser verdade a partir da M1. Reverter só o frontend hoje faria o vanilla chamar um Worker que já não tem `GET /api/field`, que já gera IDs de chamado em UUID (não mais formato ClickUp) e que já não entende a chave de push antiga — ou seja, **o rollback de frontend documentado na F5 não reverte mais o comportamento pra "como era antes"**, porque o backend evoluiu independente por baixo. Não é urgente corrigir (rollback de frontend não foi cogitado nem precisado até agora), mas registrado pra não confundir uma decisão futura. Ver "D1 + R2 como backend" pro detalhe.
  - **Verificado em produção**: as 4 rotas removidas (`migrate-d1`/`migrate-solicitantes`/`migrate-anexos`/`webhook`) devolvem 404 (com uma checagem inicial pegando resposta cacheada da borda por alguns segundos — mesmo achado de propagação já visto em fases anteriores, resolvido esperando e checando de novo); `GET /api/solicitantes` continua devolvendo a lista real (43 nomes); `POST /admin/tasks/:id` continua exigindo `ADMIN_SECRET`.
  - **Testes**: `seedChamadoComId` (helper local, grava direto no D1 com um id escolhido) substitui a semeadura via `/admin/migrate-d1` nos pontos que precisavam de fixtures com id fixo e conhecido (não dá mais pra usar `d1CreateChamado` puro pra isso, já que ele sempre gera UUID). Mock global de `fetch` virou um guarda-costas que lança erro se qualquer coisa tentar chamar a ClickUp de verdade — no lugar do mock elaborado de `FAKE_TASKS`/`FAKE_OPTIONS`, que não tinha mais nada real pra simular. Testes dos recursos removidos (migração/paginação/backfill-via-remigração) removidos junto; testes que dependiam desses fixtures reescritos pra semear direto. **180 testes no total (53+86+34+7), 0 falhando** — build do frontend limpo, zero mudança de novo (o contrato de resposta já era D1-native desde a B7/M3).
- **🔔 Alerta de chamado novo no painel de admin (2026-08-13)** — pedido do usuário logo depois da migração de saída da ClickUp: "como faço pra saber quando um chamado for criado, se não uso mais a ClickUp?". Esclarecido em 2 rodadas de `AskUserQuestion`: os 3 mecanismos juntos (push do navegador + badge dentro do painel + som), e a "caixa" precisava ser uma tela com o chamado + descrição + botão grande pra aceitar (não só um link de WhatsApp). Planejado formalmente (`EnterPlanMode`, 2 agentes Explore em paralelo pesquisando a infra de push existente e a estrutura do `AdminApp`) antes de implementar.
  - **`POST /admin/subscribe`** (`handleAdminSubscribe`, novo) — protegida por `isAdmin()` como toda rota `/admin/*`. **Sem login por pessoa no admin** (só `ADMIN_SECRET` compartilhado, sem "sou o Everson"/"sou o Henrique" nenhum) — a inscrição de push é por **navegador/dispositivo**: `id` vem do cliente (`crypto.randomUUID()`, persistido em `localStorage`), grava `adminsub_<id>` no KV `SUBSCRIPTIONS`. Reenviar com o mesmo `id` sobrescreve (idempotente).
  - **`handleCreateTask` ganhou `notifyAdminsNovoChamado`** — depois que o chamado é gravado no D1 com sucesso, enumera todo mundo sob `adminsub_` (mesmo padrão de `.list({prefix, cursor})` que `handleAdminListUsers` já usa pra `auth_`) e manda `sendWebPush` em paralelo (`Promise.allSettled`) pra cada um. Nunca derruba a criação do chamado se o push falhar (mesmo espírito de `d1TransitionStatus` — try/catch isolado). Subscription morta (404/410 — navegador desinstalado, etc.) é **apagada na hora**, em vez de ficar tentando pra sempre a cada chamado novo.
  - **Service Worker separado só pro admin** (`frontend/public/admin-sw.js`) — arquivo estático, JS puro, sem Workbox/build nenhum. **Decisão deliberada de não reaproveitar `sw.ts`/`sw.js` do app de solicitantes**: `admin.html` é documentado pra nunca virar PWA ("fora do manifest.json/**sw.js**") — um Service Worker só de push (sem precache, sem manifest, sem install-ability) é uma capacidade diferente de "virar PWA instalável", mas reaproveitar o MESMO arquivo que o app de solicitantes usa arriscaria essa distinção ficar confusa no futuro. Registrado manualmente (`navigator.serviceWorker.register('admin-sw.js', ...)`, não via `vite-plugin-pwa`) dentro de `use-admin-push-notifications.tsx`. Manda `postMessage` pros clients abertos assim que recebe um push, pra a aba já aberta reagir na hora em vez de esperar o próximo ciclo de poll.
  - **Badge (contador)**: sino + número fixo no header (`app-header.tsx`), sempre visível em qualquer seção (Gestão/Dashboard/Usuários) — reaproveita `GET /admin/tasks?status=aberto`, que já existia (sem endpoint novo). É a contagem TOTAL de chamados "aberto" — sobe na criação, desce sozinha quando aceito, **sem estado "lido/não lido" nenhum** (mais simples e sempre correto).
  - **Detecção de "chamado novo"** (`use-novos-chamados.tsx`) — diferente do badge: um `Set` local (ref, não servidor) com os ids de "aberto" já vistos. Poll a cada 20s; a **primeira carga só registra os ids em silêncio** (não alerta pra chamados que já estavam abertos antes da TI abrir o painel) — só ids que aparecem DEPOIS disso disparam som + entram na fila da "caixa".
  - **Som gerado por código** (`lib/sound.ts`, Web Audio API/`OscillatorNode`) — dois tons curtos, sem arquivo de áudio nenhum pra baixar/versionar/hospedar.
  - **A "caixa" (`NovoChamadoAlert`)** — `Dialog` com nome do solicitante, tipo, setor, descrição, e um botão grande "Aceitar chamado" que chama a MESMA mutation (`postTaskUpdate(secret, id, { status: 'em atendimento' })`) que o modal "Gerenciar" já usa — reaproveite total, nenhuma rota nova precisou pra isso. Se a fila tiver mais de 1, mostra um de cada vez com indicador "+N na fila".
  - 🛡️ **Achado real, sem relação direta com esta feature — só ficou visível testando**: `d1TransitionStatus` tinha um bug de verdade. Ao sair de "pendente" pra "em atendimento", o bloco que calcula o ajuste de due_date (`chamado.due_date + tempo pausado`, correto) rodava ANTES do bloco de "em atendimento" — que sempre recalculava `patch.dueDate = now + timeEstimate` por cima, mesmo numa retomada de pausa, sobrescrevendo o ajuste certo. Resultado: resumir de uma pausa resetava o prazo pra uma janela cheia nova (ex.: mais 1h inteira pra Normal) em vez de só empurrar pelo tempo que ficou pendente — o chamado "ganhava" SLA de graça toda vez que pausava/retomava. Corrigido: o padrão por prioridade só se aplica quando NÃO é uma retomada (`!saiuDePendente`). Achado com um teste que ficou flaky por sorte de timing (às vezes os dois valores calculados coincidiam por acaso) — não é um bug introduzido nesta sessão, já existia desde a Fase M4.
  - **Testes**: `POST /admin/subscribe` (403 sem secret, 400 sem `id`/sem `subscription`, grava/sobrescreve idempotente); `handleCreateTask` dispara push pro admin inscrito (usa o mesmo par VAPID descartável + endpoint de push mockado que `tests/d1-layer.test.js` já usa — mesma limitação de sempre, não dá pra ler o payload criptografado de volta, só confirma URL + header VAPID); assinante morto (410) é removido sem impedir push pros outros nem a criação do chamado; sem ninguém inscrito, criar chamado não falha. Guarda-costas de `fetch` do arquivo (ver Fase M5) ganhou uma exceção deliberada pra `https://fake-push-endpoint.test/*` (o "serviço de push" mockado, não é ClickUp). 187 testes no total nesta rodada inicial (53+93+34+7) — ver auditoria do `revisor` logo abaixo pro número final, depois dos testes novos que os achados dela motivaram.
  - **Verificação visual**: build de produção real (`npm run build`) + Playwright com rede mockada (fake `ADMIN_SECRET`, `GET /admin/tasks?status=aberto` mockado com contagem controlável) — confirmado: badge aparece com a contagem certa já na 1ª carga, caixa "Chamado novo" aparece quando a contagem sobe (simulado via refetch manual), botão "Aceitar chamado" visível e funcional, zero erro de console. **Achado de tooling, não do app**: `vite preview` serve em `base: '/'`, mas o build de produção deste projeto usa `base: '/chamados-ti-pwa/'` (GitHub Pages é um project site) — os dois não batem, causando 404 de asset ao tentar prever localmente sem ajuste. Contornado servindo `dist/` copiado pra um subcaminho `chamados-ti-pwa/` com um servidor estático simples, em vez de `vite preview` puro.
  - **Verificado em produção**: `POST /admin/subscribe` responde (403 sem secret — auth ativa), `admin-sw.js` publicado e acessível, `admin.html` continua sem `<link rel="manifest">`, página carrega sem erro de console.
  - 🛡️ **Auditoria do agente `revisor` (2026-08-13, mesmo dia) — 1 achado crítico e 3 avisos, todos corrigidos no mesmo dia:**
    - 🔴 **Crítico, confirmado inspecionando o bundle de produção real**: `admin-sw.js` e `sw.js` registravam no **mesmo escopo** (`/chamados-ti-pwa/`) — `admin.html`/`index.html` são MPA, arquivos irmãos no mesmo diretório, e só pode existir **1** Service Worker Registration ativa por escopo; quem registrasse por último sobrescrevia o outro silenciosamente (`self.skipWaiting()`+`self.clients.claim()` nos dois torna a troca imediata, sem confirmação). Cenário real plausível: a própria TI abre `index.html` (como qualquer colaborador, pra abrir um chamado) e `admin.html` no mesmo navegador — dependendo da ordem, o app de solicitantes perdia precache/offline/roteamento de notificação, OU o admin passava a ter suas notificações roteadas pelo SW errado. **Corrigido**: `admin-sw.js` registra com um escopo **próprio**, um sub-caminho que nenhuma página realmente visita (`${base}admin-push-scope/`) — funciona porque push/`showNotification` não dependem deste SW "controlar" `admin.html` (controle de página só importa pra interceptar `fetch`, que este SW nunca faz). Removida a dependência de `navigator.serviceWorker.ready` (nunca resolveria pra este SW, já que ele não controla nenhuma página de verdade) — espera a registration ficar `activated` direto. `admin-sw.js` ganhou `getAdminUrl()`, que reconstrói a URL real de `admin.html` a partir do escopo fake (usada no `postMessage`/`notificationclick`, que antes dependiam de `self.registration.scope` apontar pra página certa — deixou de apontar). Verificado no bundle compilado: `admin-sw.js` registra com `${base}admin-push-scope/`, `sw.js` continua em `${base}` puro — escopos distintos confirmados.
    - 🟡 **Fix de SLA da véspera cobria só parte dos casos**: a correção original (`!saiuDePendente`) tratava só "pausou depois de já estar em atendimento" — mas o painel aceita qualquer transição de status a qualquer momento (Kanban livre, limitação já aceita), então "Aberto → Pendente → Em Atendimento" (pausar ANTES de nunca ter começado a atender) é uma sequência real, e nesse caso o `due_date` deveria seguir o padrão de finalização da prioridade, não o `due_date` de aceitação mais o tempo pausado. Corrigido trocando a condição por `retomandoAtendimentoPausado = saiuDePendente && chamado.start_date != null` — `start_date` só é não-nulo depois que o chamado já entrou em "em atendimento" pelo menos uma vez, e é isso que distingue uma retomada de verdade de uma 1ª entrada com pausa no meio.
    - 🟡 **`use-novos-chamados.tsx` guardava um `Set` que só crescia, nunca esquecia** — um chamado reaberto depois de já ter passado por "aberto" uma vez nunca mais disparava som/caixa de novo (só o badge refletiria a volta dele, em silêncio). Corrigido trocando por diff contra o resultado do **poll anterior** (substituído a cada rodada, não acumulado) — mais simples e corrige o caso de reabertura de graça.
    - 🟡 **`handleAdminSubscribe` validava só `subscription.endpoint`**, não `subscription.keys.p256dh`/`.auth` — uma subscription incompleta ficaria gravada e `sendWebPush` quebraria com um erro genérico (não reconhecido pelo regex de "Push endpoint 404/410"), então nunca seria limpa, ficando falhando pra sempre em silêncio a cada chamado novo criado. Corrigido validando os 3 campos antes de gravar.
    - **Achado ao escrever o teste do 1º achado de SLA**: o teste original ("pendente pausa o SLA...") criava a fixture com `status: 'em atendimento'` mas sem `start_date` (já que `d1CreateChamado` sempre grava `start_date: null`, ignora `data.start_date`) — um estado que não existe na prática, e que fazia ESSE teste ficar flaky depois do fix acima (pelo mesmo motivo original, só que pela fixture agora). Corrigido ajustando `start_date` direto via SQL depois de criar.
    - **189 testes no total (53+95+34+7), 0 falhando, estável em 5 rodadas seguidas** (a suite tinha ficado flaky nesse meio-tempo — resolvido, ver achados acima). Build do frontend limpo, verificado com Playwright + inspeção direta do bundle compilado. Deploy real + verificado em produção (rota validando `keys` de novo, `admin-sw.js` publicado com o novo escopo).
- **📦 Versionamento retroativo aplicado (2026-08-13)** — usuário pediu, depois de perguntar "está versionando???" e descobrir que não: `frontend/package.json` estava travado em `0.0.0` desde sempre, sem tag git nenhuma, e o `<meta name="app-version">` do `index.html` vanilla (o esquema antigo, ver "Padrões de desenvolvimento") tinha parado de ser atualizado em `0.3.5`, congelado desde antes da Fase F5 — 55 commits inteiros (B2 até a auditoria do alerta de admin) foram feitos sem nenhum número de versão. Pedido explícito: versionar tudo retroativamente **conforme a diretriz já documentada** (SemVer + Conventional Commits) e **aplicar 1.0.0 no exato momento em que a migração de saída da ClickUp terminou** (pós-React, pós-D1/R2 — o momento em que o sistema passou a ser 100% autônomo, sem nenhuma dependência de API externa).
  - **Convenção usada, mecânica e sem exceção** (pra não depender de julgamento caso a caso em 55 commits): todo commit `feat:` sobe MINOR (reseta PATCH pra 0); todo `fix:` sobe PATCH; `docs:`/`chore:`/`refactor:` não sobem versão sozinhos — ficam sob a versão do commit anterior. Tag anotada (`git tag -a`) no **último commit ainda "dentro" daquela versão** (ex.: um `feat:` seguido de `docs:` — a tag fica no `docs:`, não no `feat:`, já que ele representa o estado completo e documentado daquela versão).
  - **30 tags criadas retroativamente**, de `v0.4.0` (primeiro commit depois do `0.3.5` congelado, sem versão nenhuma até agora) até `v1.1.0` — cobrindo B2-B6, F1-F5, B7 partes 1-2, M1-M5 e a feature de alerta de admin, na ordem cronológica real do histórico (`git log --reverse`). **`v1.0.0` foi aplicada deliberadamente no commit `c384154`** ("docs: documenta a conclusão da migração de saída da ClickUp — Fase M5") — não é o resultado mecânico da regra (que daria `0.25.0` nesse ponto); é uma decisão explícita do usuário, sobrepondo a mecânica só nesse ponto, marcando o fim do período pré-produção (`MAJOR = 0`) exatamente onde o sistema deixou de depender de qualquer API externa.
  - **`frontend/package.json`** (campo `version`) é o novo local vivo da versão declarada — substitui o `<meta name="app-version">` do `index.html` vanilla, que fica congelado em `0.3.5` como parte do artefato de rollback (não é mais atualizado, nem deveria ser — representa o estado real daqueles arquivos quando pararam de ser produção). Ver "Padrões de desenvolvimento" pra convenção completa.
  - Tags são só locais até serem enviadas com `git push --tags` (ou `git push origin <tag>`) — feito no mesmo commit que aplicou essa mudança.
- **D1 + R2 como backend** — cada chamado é uma linha no D1 (`chamados`, com `chamado_assignees`/`chamado_anexos` relacionadas), anexo (bytes) no R2. Nenhuma chamada à API da ClickUp em nenhuma rota desde a Fase M5 (2026-08-13) — a conta continua existindo, intacta, como arquivo. O frontend fala com `WORKER_URL/api/*`, autenticado por `APP_SHARED_SECRET` (header `X-App-Secret`), que o Worker valida contra `env.SUBSCRIBE_SECRET`.
- **Zero dependências** — HTML/CSS/JS puro, sem framework/bundler (decisão explícita). Os testes usam só `node vm`/`fetch`/`node:sqlite`/`assert` nativos do Node, nada instalado — `package.json` (2026-08-11) existe só pelo atalho `npm test`, sem campo `dependencies` nenhum.
- **Push e proxy da ClickUp desacoplados** no mesmo Cloudflare Worker (`push-worker.js`, deploy automatizado via Wrangler + GitHub Actions desde 2026-08-10 — ver "Deploy do Worker"); `VAPID_PUBLIC_KEY`/`APP_SHARED_SECRET` hardcoded em `app.js` por serem identificadores públicos/de baixo risco (não dão acesso à ClickUp por si só).
- **Contratos de sincronização** que devem permanecer idênticos entre `app.js` e `push-worker.js`: chaves de `STATUS_MAP` ↔ `NOTIFY_STATUSES`, e o field_id de `SOLICITANTE` em `FIELD_IDS`.
- **Prioridade nunca é manual** — sempre derivada do tipo; não expor seletor de prioridade ao usuário.
- **Login com senha, sem banco novo** — reaproveita o KV do Worker (`auth_<nome>`, `session_<token>`); senha em PBKDF2-SHA256 com formato autodescritivo (dá pra trocar de algoritmo/migrar de storage no futuro sem invalidar senha de ninguém — decisão tomada em 2026-07-24 pensando em evolução sem perda de acesso). Identidade sempre resolvida no servidor a partir da sessão, nunca do que o cliente manda — é isso que impede um solicitante ver/criar chamado como outro.
- **Lista de solicitantes buscada em runtime da ClickUp** — decisão tomada em 2026-07-23 após bug de nome trocado causado pela lista fixa desincronizar (ver `LEGACY_USER_IDX_TO_NAME` pra contexto da migração). Adicionar colaborador agora é só no ClickUp, sem tocar no código.
- **Versão declarada em três lugares** a manter sincronizados: `<meta name="app-version">` e o `<footer>` no `index.html`, e `APP_VERSION` no `sw.js` (esse último precisa mudar mesmo em fixes só de `app.js`, pra forçar a invalidação do cache do Service Worker). `admin.html`/`admin.js` ficam de fora dessa obrigação — página separada, sem versão própria declarada.
- **`ADMIN_SECRET` nunca é hardcoded em `admin.js`** — decisão tomada ao construir a Fase 2 (2026-08-06): diferente do `APP_SHARED_SECRET` (público por design, só libera o proxy), o segredo de admin dá acesso a dados de todo mundo, então não pode existir em código publicado no GitHub Pages. `admin.html` pede o segredo numa tela de gate e guarda só no `localStorage` de quem digitou.
- **Banco Cloudflare D1 provisionado em 2026-08-10, mas ainda não usado** — explorado como alternativa/complemento à ClickUp como backend (avaliado ao lado de Supabase e Turso; D1 escolhido pra esse teste por já ser nativo da mesma conta/plataforma do Worker, sem serviço/conta externa nova). Binding `CHAMADOS_DB` em `wrangler.toml` (banco `chamados-ti-db`). **Nenhuma lógica de chamado usa isso ainda** — `push-worker.js` continua 100% ClickUp como fonte de verdade. Migrar de fato é decisão futura separada, não tomada ainda (ver "Próximos passos").
- **🚀 B7 parte 2 concluída — fase 1 e fase 2 (2026-08-12): schema do D1 passa a suportar múltiplos operadores por chamado, e a leitura de `GET /admin/tasks`/`GET /admin/metrics` já corta pra usar isso.** Decisão original de dividir em 2 fases (pedido do usuário, orçamento de tokens da sessão perto do fim): fase 1 = só schema aditivo + captura completa nas escritas + backfill do histórico, zero mudança de leitura; fase 2 = cortar `GET /admin/tasks`/`GET /admin/metrics` pra ler dessa tabela nova — usuário pediu pra seguir pra fase 2 na sequência, mesma sessão.
  - **Nova tabela `chamado_assignees`** (`chamado_id`, `assignee_id`, chave primária composta — N-pra-N de verdade, igual `assignees[]` da ClickUp) em `d1/schema.sql`. **Sem `FOREIGN KEY` de propósito** — `node:sqlite` (e possivelmente o D1 real) vem com `PRAGMA foreign_keys` ligado por padrão, o que bloquearia qualquer migração futura que precise recriar a tabela `chamados` (o padrão já usado em `POST /admin/migrate-schema-nullable-tipo-setor`); sem cascade/delete de chamado no app hoje, não há necessidade real de enforcement. Regra nova pro projeto: **não usar `REFERENCES` no schema do D1** por esse motivo.
  - **`mapClickUpTaskToD1` ganhou `assignee_ids`** (array completo de `task.assignees`), ao lado do `assignee_id` que já existia (primeiro só, continua em uso por `GET /api/my-tasks`). **`d1SetAssignees(env, chamadoId, assigneeIds)`** substitui por completo quem está atribuído (DELETE + INSERT, não tenta calcular diff — mesmo espírito simples do resto da camada D1), chamada em toda criação (`handleCreateTask`), atualização de operador (`handleAdminUpdateTask`, só quando o campo é tocado — mesma proteção de 2026-08-07) e migração de histórico (`d1MigrateChamado`).
  - **Backfill do histórico reaproveitou o próprio `POST /admin/migrate-d1`** (sem rota nova pra isso) — `d1MigrateChamado` passou a chamar `d1SetAssignees` **incondicionalmente**, mesmo quando o `INSERT OR IGNORE` da linha principal é ignorado (chamado já existia) — rodar a migração de novo virou também o mecanismo de backfill do 2º+ operador de quem já estava no D1 desde antes dessa tabela existir. **Motivo de a fase 1 já valer a pena sozinha:** `mapClickUpTaskToD1` sempre só capturou `assignees[0]` — ou seja, mesmo os "453/453" migrados na parte 1 (ver abaixo) já tinham perdido silenciosamente o 2º+ operador de qualquer chamado que tivesse mais de um atribuído direto pela ClickUp.
  - 🛡️ **Achado real rodando o backfill em produção:** a primeira tentativa (1 DELETE + N INSERT sequenciais por chamado, cada `.run()` um subrequest do Worker) estourou o teto de subrequests por invocação (~1000 no plano pago), erro real da Cloudflare `"Too many API requests by single Worker invocation"`, na volta de ~332 dos 453 chamados. Corrigido usando `env.CHAMADOS_DB.batch([...])` (manda DELETE + todos os INSERT num round-trip só, contando como 1 subrequest em vez de 1+N) — `d1SetAssignees` caiu pra 1 subrequest fixo, `d1MigrateChamado` pra 2/linha (insert principal + batch de assignees). Reprocessado com sucesso total depois do fix: **454/454 chamados, `migrated:0, skipped:454, errors:[]`**. Nova rota-padrão pro projeto: qualquer operação em lote no D1 que processe volume real deve usar `.batch()`, não `.run()` sequencial — o teto de subrequests é por invocação do Worker, não por request HTTP externo. Mocks de D1 (`makeD1FromSqlite` em `tests/d1-layer.test.js`/`tests/push-worker.test.js`) ganharam um adaptador `.batch()` equivalente.
  - **Rota de criação da tabela** (`POST /admin/migrate-schema-chamado-assignees`, `ADMIN_SECRET`) — idempotente, só `CREATE TABLE IF NOT EXISTS` + índice, sem a complexidade de recriar tabela (tabela nova, não precisa preservar dado nenhum). Rodada uma vez em produção antes do backfill.
  - **Verificado em produção:** chamado real `86af8erzt` ("Atolamento de papel") tem 2 operadores atribuídos na ClickUp (`[200498355, 112089386]`) — exemplo concreto que motivou e confirma o trabalho; passou pelo backfill sem erro (0 erros no total). Sem endpoint de leitura pra essa tabela ainda (é a fase 2), então a confirmação é indireta: o backfill inteiro rodou sem nenhum erro reportado, e `d1SetAssignees` só retorna sem lançar exceção quando o `.batch()` de fato commitou.
  - 151 testes no total (53+66+25+7) — 5 novos em `tests/push-worker.test.js` cobrindo criação da tabela (403 sem segredo, idempotência), migração de chamado com 2 assignees populando as 2 linhas, backfill restaurando assignees apagados, e `POST /admin/tasks/:id` com `assigneeId` único substituindo por completo a tabela de junção.
  - **Fase 2 (mesmo dia, na sequência — usuário pediu pra continuar): `GET /admin/tasks`/`GET /admin/metrics` trocam `fetchAllTasks` (paginação da ClickUp) por `d1ListChamados`/`d1GetMetrics`.** `d1ListChamados` ganhou o parâmetro `withAssignees` (opt-in — só as rotas de admin pedem, `GET /api/my-tasks` continua sem pedir, zero mudança de custo/comportamento na rota mais chamada do Worker); quando pedido, busca os assignees de todos os chamados retornados via `d1GetAssigneesMap` (nova função) e anexa `row.assignee_ids`, que `d1RowToTaskShape` usa pra reconstruir `assignees[]` completo (cai pro `assignee_id` único quando `assignee_ids` não foi pedido). `d1GetMetrics.tempoMedioPorOperador` passou a usar a mesma função pra atribuir a duração INTEIRA a cada operador de um chamado encerrado (mesma regra de sempre — não dividida entre eles); `nome` sempre `null` agora (D1 não guarda nome de operador, só id — o painel já resolve via `OPERADORES` no cliente, comportamento já esperado). Filtro `solicitante` ficou mais simples: D1 já guarda o nome resolvido direto na coluna `solicitante`, não precisa mais do passo de resolução nome→orderindex que a versão ClickUp exigia (`getSolicitanteMaps`). `fetchAllTasks` continua existindo — ainda usada por `POST /admin/migrate-d1` (a migração de histórico ainda lê da ClickUp, faz sentido, é dali que os dados vêm).
    - 🛡️ **Achado real em produção, corrigido em minutos no mesmo dia:** o primeiro deploy da fase 2 quebrou as duas rotas com **500 puro** (`error code: 1101`, exceção não tratada da Cloudflare) sempre que o resultado batia em centenas de linhas (`/admin/metrics` sem filtro, `/admin/tasks?status=encerrado`) — `d1GetAssigneesMap` mandava TODOS os ids de chamado num `IN (...)` só, estourando o limite de variáveis bindáveis por statement do SQLite/D1. Descoberto testando direto contra produção logo depois do deploy (filtro pequeno funcionava, filtro grande dava 500) — não foi pego pelos testes locais porque o dataset de teste (2-3 chamados) nunca chegava perto de qualquer teto. Corrigido dividindo em pedaços de 50 ids por query (`CHUNK`, conservador de propósito). Teste novo em `tests/d1-layer.test.js` (60 chamados, além de 1 pedaço) — pegaria essa classe de bug antes do próximo deploy parecido.
    - **Verificado ao vivo em produção depois do fix:** `/admin/tasks`/`/admin/metrics` voltaram a 200; `total: 454` bate com o backfill; chamado real `86af8erzt` ("Atolamento de papel") aparece em `/admin/tasks` com os **2 operadores reais** (`[112089386, 200498355]`) — o cenário exato que motivou toda a B7 parte 2, confirmado funcionando de ponta a ponta.
    - 153 testes no total (53+67+26+7) — testes de `/admin/tasks`/`/admin/metrics` reescritos pra usar um env de D1 isolado (o `env` global do arquivo já acumula chamados de outros testes via dual-write — só passou a importar depois que essas rotas leem do D1 de verdade), mais teste do array completo de assignees e teste de volume (60 chamados) pro chunking.
  - 🛡️ **Auditoria do agente `revisor` (2026-08-12) sobre a B7 parte 2 inteira** — nada crítico (sem SQL injection, todas as queries usam bind parametrizado; sem off-by-one no chunking; `GET /api/my-tasks` confirmado não pagando o custo de `withAssignees`). Dois achados de consistência, corrigidos na hora:
    - **`d1UpdateChamado` ganhou o parâmetro opcional `assigneeIdsForSync`** — quando presente, o `UPDATE chamados` e a substituição de `chamado_assignees` (via `buildSetAssigneesStatements`, extraído de `d1SetAssignees`) viram **1 `.batch()` só**, atômico. Antes eram 2 chamadas D1 separadas dentro do mesmo try/catch best-effort de `handleAdminUpdateTask` — se a 1ª tivesse sucesso e a 2ª falhasse (erro transiente do D1), as duas fontes divergiam **silenciosamente**. Isso já era uma característica aceita do espelho best-effort desde a B7 parte 1, mas o raio de impacto mudou: antes, uma divergência só afetava `GET /api/my-tasks` (as rotas de admin ainda liam 100% da ClickUp, "auto-curando" a cada carregamento); agora que `GET /admin/tasks`/`GET /admin/metrics` leem exclusivamente de `chamado_assignees`, a divergência ficaria presa até o próximo toque manual no operador — o painel que a TI usa pra decisão (filtro por operador, tempo médio de atendimento) podia ficar errado sem nenhum sinal de erro. `d1CreateChamado` recebeu o mesmo tratamento (ainda dormente, sem rota usando — ver Fase B2) e ganhou `data.assignee_ids` (array completo, mesmo padrão de `mapClickUpTaskToD1`), pra não reintroduzir a limitação de 1-operador-só se um dia for ligada a um handler real.
    - **Cobertura de teste**: o teste que alegava cobrir "array completo de assignees" em `GET /admin/tasks` só exercitava um chamado com 1 assignee — o cenário de 2+ nunca passava pela rota HTTP de verdade (só checagens diretas contra `d1GetChamado`/a tabela de junção). Corrigido com um teste que filtra pelo 2º operador de um chamado real com 2 assignees e confirma o array completo voltando por `GET /admin/tasks`. Teste novo também pra atomicidade (simula falha de `.batch()` no meio de uma atualização admin — confirma que nem `status` nem `assignee_id`/`chamado_assignees` mudam, tudo ou nada).
    - **Limitação conhecida, documentada e não corrigida nesta rodada:** com o fix do `.batch()`, `POST /admin/migrate-d1` usa 2 subrequests/linha (INSERT principal + batch de assignees) — pros 454 chamados reais isso já soma ~908 subrequests numa invocação só, abaixo do teto de ~1000/invocação do plano pago, mas com margem apertada. Conforme o volume de chamados crescer, essa migração completa vai estourar esse teto de novo, do mesmo jeito que estourou antes do fix desta sessão. Não é um bug ativo hoje (454/454 confirmado sem erro) — solução futura, se/quando precisar: paginar a migração em lotes de N chamados por invocação (query params tipo `?offset=&limit=`), ou também usar `.batch()` pros INSERTs principais (reduziria pra ~1 subrequest/linha).
    - **Observação lateral, não é regressão desta sessão:** o `<Sidebar>` do shadcn (`components/ui/sidebar.tsx`, não modificado aqui) não repassa o `className` do componente pro branch mobile (`<SheetContent>`) — o gradiente restaurado no fix visual (e qualquer `className` customizado, antes disso) só aplica no desktop/icon-collapsible, que é o que o painel de admin de fato usa hoje. Fica documentado caso o uso mobile do admin vire prioridade algum dia.
- **🚀 Fase B7 concluída — parte 1 (2026-08-12): D1 vira fonte de verdade pra `GET /api/my-tasks` (o endpoint mais chamado do Worker — poll de 60s de cada solicitante logado) e passa a ser espelhado em tempo real em toda escrita.** Espelho temporário (decisão de 2026-08-11): ClickUp continua recebendo 100% das escritas normalmente — `handleCreateTask` cria na ClickUp (que continua gerando o `task_id` e recebendo o anexo) e espelha o resultado no D1 logo depois (`d1MigrateChamado`, best-effort — falha no espelho nunca derruba a resposta pro usuário); `handleAdminUpdateTask` espelha status/solução/operador no D1 só depois que TODAS as sub-mutações já foram aplicadas com sucesso na ClickUp. `mapClickUpTaskToD1` deixou de exigir SOLICITANTE resolvido — migra com `solicitante = ''` (não é possível `NULL`, a coluna é `NOT NULL`) em vez de virar erro, fechando a lacuna dos 269 chamados de antes do app existir (ver "Fase B3"). Migração completa rodada em produção: **441 de 453 chamados reais migrados** (178 já vinham da B3 + 263 novos com o fix do SOLICITANTE vazio); os 12 restantes falhavam por um motivo genuinamente diferente (`campo TIPO ausente na task`).
  - ✅ **Os 12 restantes fechados no mesmo dia, a pedido do usuário** ("consegue deixá-los vazios e subir pro banco de dados, pra ter o total?"). Investigado antes de mexer: essas 12 tasks (bem antigas) não tinham custom_fields nenhum preenchido — nem TIPO nem SETOR (prioridade e o resto, normais). `tipo`/`setor` deixaram de ser `NOT NULL` no `d1/schema.sql` (SQLite/D1 não suportam remover `NOT NULL` via `ALTER` direto — nova rota de uso único, `POST /admin/migrate-schema-nullable-tipo-setor`, recria a tabela: cria a nova com o schema certo, copia os dados com checagem de contagem antes de apagar a antiga, renomeia, recria os índices; idempotente). `mapClickUpTaskToD1` migra tipo/setor ausentes como `NULL` de verdade (não string vazia, já que a coluna aceita `NULL` agora); `d1RowToTaskShape` omite a entrada de `custom_fields` correspondente em vez de mandar `value: null` — o frontend faz `Number(getCF(...))`, e `Number(null)` vira `0` (bateria com o orderindex 0 de verdade), não `NaN`/"—" como seria o esperado pra "campo ausente". **Resultado: 453 de 453 chamados reais no D1, zero erro, confirmado idempotente** (`migrated:0, skipped:453`).
  - 🛡️ **Por que só `GET /api/my-tasks` — e não `GET /admin/tasks`/`GET /admin/metrics`/`GET /tasks/:id` — cortou a leitura pro D1 nesta fase:** analisado com cuidado antes de decidir, não é um esquecimento.
    - **`GET /tasks/:id`** devolve `attachments`, e o D1 não guarda isso (upload de anexo continua 100% ClickUp desde a Fase B4/F4.5) — cortar essa rota pro D1 quebraria a visualização de anexo que acabou de ser construída na F4.5.
    - **`GET /admin/tasks`/`GET /admin/metrics`** são a tela que a TI usa pra trabalhar (Quadro/Tabela/Dashboard) — e o schema do D1 (`assignee_id INTEGER`, uma coluna só) **não representa chamado com 2+ operadores atribuídos**, ao contrário da ClickUp (`assignees[]`). O painel de admin tem uma proteção real construída especificamente pra esse cenário (aviso de "múltiplos operadores atribuídos" no modal "Gerenciar", ver Fase 4 do painel de admin) — cortar a leitura pro D1 exibiria só o primeiro operador pra qualquer chamado aberto/pendente com 2+ atribuídos, quebrando essa proteção em silêncio bem na tela onde isso mais importa. Novos chamados criados pelo app sempre têm exatamente 1 operador (o formulário só permite escolher 1), e o próprio `handleAdminUpdateTask` sempre colapsa pra 0 ou 1 assignee quando o campo operador é tocado — então essa limitação só afeta chamados que já tinham 2+ atribuídos direto pela ClickUp antes de qualquer mutação por aqui. Estreito, mas real o bastante pra não arriscar sem uma extensão de schema. **A extensão em si (tabela `chamado_assignees`) já foi construída na B7 parte 2 fase 1 (2026-08-12)** — o que falta agora é só trocar a leitura dessas duas rotas pra usá-la (fase 2, ver "Próximos passos").
  - **Automação de SLA (`d1TransitionStatus`, pronta desde a Fase B5) continua sem uso** — `handleAdminUpdateTask` só espelha os campos literais (status/solução/operador), não recalcula `start_date`/`due_date`/`date_closed` no D1. Essa lógica continua vindo só da automação configurada na ClickUp (via webhook, `runStatusAutomation`) — rodar as duas em paralelo enviaria push duplicado pro mesmo evento. Cortar isso de vez também fica pra uma B7 parte 2 futura.
- **🚀 Fase F5 concluída (2026-08-12): corte de produção do frontend — o build React (`frontend/`) é o que está no ar.** Build MPA (`admin.html`+`index.html` como entradas reais, `main.tsx`/`main-admin.tsx`), `base` do Vite condicional pro subpath do GitHub Pages (`/chamados-ti-pwa/`), `scripts/fix-admin-html.mjs` removendo o `<link rel="manifest">` que o `vite-plugin-pwa` injetava em `admin.html` sem distinção. Pipeline novo (`deploy-frontend.yml`, mesmo padrão do Worker). **Achado real e documentado em "Deploy do Frontend":** `actions/deploy-pages` publica de verdade assim que roda, independente do `build_type` configurado no repositório — o primeiro push desta fase (só pra validar o build no CI) já cortou produção sozinho, mais cedo do que o planejado. Verificado depois, ao vivo, contra o Worker/ClickUp reais (não mockado): 452 chamados carregando certo no Quadro/Tabela/Dashboard do admin, lista real de solicitantes na tela de login, zero erro de console. `build_type` corrigido pra `workflow` na sequência, deixando a configuração consistente (evita reversão acidental por um push futuro em outro arquivo). Arquivos vanilla continuam no repo como rollback (ver "Estrutura de arquivos"). Decisões de execução já tomadas em 2026-08-11 (mesma estrutura de páginas, corte dos dois apps junto) seguidas à risca.
- **Fase B2 do roadmap de modernização concluída (2026-08-11): camada de dados D1 escrita, testada, sem nenhuma rota usando ainda.** Schema em `d1/schema.sql` (tabela `chamados`, já aplicado no banco real via API da Cloudflare — confirmado com smoke test de CRUD completo direto no D1 de produção). Funções `d1CreateChamado`/`d1GetChamado`/`d1ListChamados`/`d1UpdateChamado`/`d1GetMetrics` em `push-worker.js`, exportadas mas não chamadas por nenhum handler — a ClickUp continua 100% a fonte de verdade em produção, isso é só preparação pra uma migração futura (B3 em diante). 16 testes novos em `tests/d1-layer.test.js`, usando `node:sqlite` (nativo do Node, zero dependência) rodando o mesmo `d1/schema.sql` real num banco em memória — testa as funções de produção de verdade, não uma reimplementação paralela. 122 testes no total (53+53+16), 0 falhando.
- **Fase F4.5 concluída (2026-08-11): anexo, push notifications e PWA/Service Worker no app dos solicitantes em React, também só local.** Fecha o gap identificado ao terminar a F4 — pré-requisito decidido pra F5 (os dois apps só trocam juntos, ver "Próximos passos"). Anexo: escolher arquivo ou colar print (Ctrl+V) no formulário (`anexo-helpers.ts`), upload via multipart depois de criar a task (`uploadAttachment` em `app-api.ts`, não derruba a abertura do chamado se falhar — aviso mostrado no modal de WhatsApp, que fica montado independente da aba); chip de anexo existente + modal de preview no `TicketCard`, buscado via `GET /tasks/:id` (única rota que devolve `attachments`), keyed por `task.id`+`date_updated` no TanStack Query (substitui o `Map` manual de cache da versão vanilla pelo cache nativo da lib). Push: `use-push-notifications.tsx` porta `subscribeToPush()`/`setupNotifications()` de `app.js` 1:1 (mesmo `VAPID_PUBLIC_KEY`, mesmo endpoint `/subscribe`, mesma janela de 24h pra reaparecer o banner). PWA: `vite-plugin-pwa` em modo `injectManifest` (não `generateSW`) — o Workbox monta a lista de precache dos assets com hash do Vite automaticamente, eliminando a classe de bug do incidente de 2026-08-10 (lista de nomes fixos escrita à mão desincronizando do build); `sw.ts` porta os handlers de push/notificationclick/fetch-bypass de `sw.js` quase 1:1, registrado via `useRegisterSW()` só quando o `SolicitanteApp` monta — `admin.html`/`AdminApp` continuam nunca sendo PWA. Deep-link por hash (`#aba:taskId`) e mensagem do SW (`OPEN_TASK`) pro clique em notificação abrir a aba/chamado certo, com o pulso visual (`.task-highlight`, agora em `index.css`).
  - 🛡️ **Achado testando esta fase:** uma vez o Service Worker está no controle da página, ele redespacha `fetch()` de dentro do próprio contexto — fora do alcance do `page.route()` do Playwright, que só intercepta no nível da página/frame. Os testes funcionais (login, anexo, banner, deep-link) rodam com o SW bloqueado nesse contexto (`serviceWorkers: 'block'`); o registro do SW em si, o manifest e o precache são verificados num contexto separado, sem mock de API. Não é um bug do app — é uma limitação de como o Playwright intercepta rede quando um SW real está ativo, documentada aqui pra não ser redescoberta.
- **Fase F4 concluída (2026-08-11): app dos solicitantes em React (`SolicitanteApp.tsx`), também só local.** Porta 1:1 de `js/app.js`: login/cadastro combinado (mesmo `/auth/login`→404→`/auth/register`), formulário Abrir Chamado com prioridade automática pelo tipo (`CATEGORIA_PRIORIDADE`), abas Meus Chamados/Histórico com card de detalhe (barra de SLA, banner de atraso, solução aplicada em destaque), modal de WhatsApp pós-abertura. `use-session-auth.tsx` é deliberadamente mais simples que `use-admin-auth.tsx`: sem ping de validação no boot, confia no `localStorage` até a primeira chamada real (igual ao `boot()` da versão vanilla). `App.tsx` virou um switcher local de dev (hash `#admin`/`#solicitante`) só pra rodar os dois apps lado a lado antes da Fase F5 decidir a arquitetura real de publicação — o conteúdo antigo de `App.tsx` foi extraído pra `AdminApp.tsx` sem mudar lógica. Fora de escopo original (fechado na Fase F4.5, ver acima): upload de anexo, push notifications, PWA/Service Worker do app dos solicitantes.
  - 🛡️ **Bug real achado testando esta fase (e corrigido em 8 componentes de `frontend/src/components/ui/`):** o gerador do shadcn CLI produziu classes Tailwind v4 do tipo `data-horizontal:`/`data-active:`/`data-checked:`/`data-open:`/`data-closed:` (seletor de atributo booleano — bate se o atributo *existir*, com qualquer valor) que nunca correspondem ao que o Radix realmente escreve no DOM (`data-orientation="horizontal"`, `data-state="active"`, e até `data-active="false"` via prop — sempre COM valor, então o seletor booleano casava de qualquer jeito). Isso quebrava por completo o layout do `Tabs` (virava `flex-row` em vez de `flex-col` — foi assim que o bug apareceu, testando a tela de abas do app de solicitantes), a cor de seleção do `RadioGroup` (operador escolhido nunca ficava visualmente marcado) e o destaque do item ativo da **sidebar do próprio painel de admin já em produção** (bug antigo do F1, não introduzido agora — só detectado agora). Corrigido em `tabs.tsx`, `radio-group.tsx`, `sidebar.tsx`, `separator.tsx`, `dialog.tsx`, `tooltip.tsx`, `select.tsx`, `sheet.tsx` trocando pra seletor de valor (`data-[orientation=horizontal]`, `data-[state=active]`, `data-[active=true]`, etc.). Verificado via inspeção real do DOM (não só visual) antes de corrigir, e reconfirmado depois com Playwright + regressão da Fase F2 (nada quebrou nos componentes compartilhados).
- **Fase F3 concluída (2026-08-11): Dashboard em React (GET /admin/metrics), também só local.** Mesmos cards de métricas, donut de SLA em SVG puro (sem lib de gráfico) e barras de volume por tipo/setor de `admin.js`, portados 1:1. Verificado no Chromium real com rede mockada.
- **Fase F2 concluída (2026-08-11): Painel de Gestão em React, já ligado nos endpoints reais (só em ambiente local).** Quadro (Kanban, drag-and-drop nativo), Tabela (agrupada por status, collapse persistido) e modal "Gerenciar" — mesma lógica de `admin.js` portada 1:1, incluindo as duas proteções do revisor (2026-08-07): `assigneeId` só entra no POST se o operador for tocado, e status desconhecido nunca vira "Aberto" em silêncio. TanStack Query cuida do fetch/poll (60s) e das mutations. Achado durante o teste: `GET /admin/tasks` também precisa tratar `403` (só as mutações tratavam) — corrigido. Verificado no Chromium real com rede mockada (nunca toca ClickUp de verdade): login, aviso de multi-operador, salvar preserva os dois assignees, drag-and-drop muda só o status, filtro dispara query certa, sessão revogada volta pro gate. `admin.html` continua sendo o painel de produção — nada mudou lá.
- **Fase F1 concluída (2026-08-11): setup do front-end React, em `frontend/` neste mesmo repositório.** Vite + React + TypeScript + Tailwind v4 + shadcn/ui. Shell visual (sidebar Gestão/Dashboard + header) espelhando `admin.html`, com design tokens que reproduzem os valores REAIS de `css/style.css`/`css/admin.css` (não paleta genérica do shadcn). Sem dado real, sem rota, sem ligação com o Worker — isso começa na Fase F2. `admin.html` continua sendo o painel de produção, sem nenhuma mudança. Build de produção verificado sem erro no Chromium real. Exceção deliberada e já decidida à filosofia zero-dependência do resto do projeto — só para esta pasta (tem `package.json`/`node_modules` próprios).
- **Fase B6 concluída (2026-08-11): infraestrutura de teste local formalizada.** Já vinha sendo construída organicamente desde a Fase B2 (`node:sqlite` pro D1, mock em memória pro R2) — B6 só fechou o que faltava: `tests/run-all.js` (roda as 4 suítes em sequência, resume o resultado, sai com código de erro se qualquer uma falhar) e `package.json` (só pelo atalho `npm test`, sem dependência nenhuma). 142 testes no total, 0 falhando.
- **Fase B5 concluída (2026-08-11): automação de SLA/push embutida no D1, sem nenhuma rota usando ainda.** `d1TransitionStatus` em `push-worker.js` reescreve `runStatusAutomation`/o envio de push do `handleWebhook` numa única função que não depende do webhook da automação da ClickUp — pausa/retomada de SLA em "pendente", `start_date`/`due_date` ao entrar em "em atendimento", `date_closed` ao "encerrado", push enviado (falha no push nunca derruba a transição). 9 testes novos em `tests/d1-layer.test.js` (usa um par VAPID descartável gerado via `crypto.subtle` nativo). 142 testes no total (53+57+25+7), 0 falhando.
- **Fase B4 concluída (2026-08-11): camada de anexos R2 escrita, testada, sem nenhuma rota usando ainda.** Bucket `chamados-ti-anexos` criado (precisou habilitar R2 na conta antes — produto separado do D1/KV, pediu aceitar termos + cartão cadastrado, mesmo ficando dentro do free tier: 10GB/mês, bem acima do volume real do projeto). Binding `ANEXOS` em `wrangler.toml`. Funções `r2UploadAnexo`/`r2GetAnexo`/`r2DeleteAnexo` em `push-worker.js` — key com prefixo `chamados/<id>/<uuid>-<nome sanitizado>` (sanitização remove separador de caminho e `..`, evita path traversal via nome de arquivo enviado pelo cliente). `handleUploadAttachment` continua 100% ClickUp — nenhum anexo real sobe pro R2 ainda. 7 testes novos em `tests/r2-layer.test.js` (mock R2 em memória). 133 testes no total (53+57+16+7), 0 falhando. CRUD completo verificado direto no bucket de produção via API (upload/download/delete).
- **Fase B3 concluída (2026-08-11): migração de histórico executada — 178 de 447 chamados importados pro D1.** `POST /admin/migrate-d1` (protegida por `ADMIN_SECRET`, aceita `{dryRun:true}` pra simular sem gravar) lê tudo da ClickUp via `fetchAllTasks` e grava no D1 com `id = task_id da ClickUp` (preserva rastreabilidade), idempotente (`INSERT OR IGNORE` — rodar de nova não duplica). **Os 269 restantes não migraram por um motivo real, não um bug**: são chamados criados antes do app entrar em produção (todos de 2026-01 a metade de 2026-05), quando o campo SOLICITANTE simplesmente não existia/não era preenchido nas tasks criadas direto na ClickUp — confirmado investigando o campo bruto (nenhum é "índice órfão", é ausência de valor mesmo). A partir de 2026-06, 100% dos chamados têm SOLICITANTE preenchido (bate exatamente com o app já estar em uso). Nada se perde — os 269 continuam intactos na ClickUp, só não entraram no D1 por enquanto. 4 testes novos em `tests/push-worker.test.js` (403 sem segredo, dry-run não grava, migração real grava os campos certos, segunda rodada é idempotente). 126 testes no total (53+57+16), 0 falhando.

## Próximos passos

1. **Concluir o teste de usabilidade** e incorporar o feedback antes de promover a versão.
2. Avaliar caminho para **v1.0** (primeiro deploy "oficial") quando a usabilidade estiver validada.
3. **`POST /webhook` não tem autenticação** — diferente de toda outra rota, aceita qualquer POST sem checar segredo/assinatura. Quem souber a URL do Worker (hardcoded em `app.js`, pública) e um `task_id` real pode forjar uma mudança de status e disparar a automação de SLA/push. Corrigir exige também mudar a URL configurada na automação do ClickUp (coordenar antes de implementar).
4. **Nota de atendimento no chamado encerrado** (sugerida, não implementada) — 1 a 5 estrelas ou 👍/👎 quando o usuário abre um chamado recém-encerrado; precisa de campo novo na ClickUp + endpoint novo no Worker.
5. **Decisão de arquitetura (2026-08-03, revisada em 2026-08-07): ClickUp continua guardando o dado, mas a TI para de usar a interface dela pra trabalhar.** A decisão original (2026-08-03) era só um painel de leitura por cima da ClickUp; em 2026-08-07 o gestor definiu o objetivo real: Everson/Henrique deixam de abrir a ClickUp no dia a dia — aceitar chamado, mudar status, escrever solução e atribuir operador passam a ser feitos direto no painel de admin, que muta a ClickUp por trás via Worker. **Não é migração de backend** (os 436+ chamados existentes, os campos customizados, a automação de SLA — nada disso muda ou é reescrito); é só a *interface de trabalho* que troca de lugar.
   - ✅ **Fase 1 (2026-08-06):** endpoints de leitura no Worker — `GET /admin/users` (já existia), mais `GET /admin/tasks` (todos os chamados, com filtros) e `GET /admin/metrics` (agregados de SLA/volume/tempo de atendimento). Ver "Autenticação". Testado, implantado e publicado.
   - ✅ **Fase 2 (2026-08-06):** frontend próprio (`admin.html`/`admin.js`/`css/admin.css`, mesma filosofia zero-dependência) consumindo esses endpoints — tela de gate pro `ADMIN_SECRET` (nunca hardcoded, ver "Painel de admin"), cards de métricas, filtros e tabela de chamados.
   - ✅ **Fase 3 (2026-08-06):** busca por título + paginação na tabela, exportação CSV e gráfico de SLA nativo (SVG puro, sem lib) — tudo client-side, sem endpoint novo no Worker.
   - 🛡️ **Auditoria do agente `revisor` (2026-08-06)** sobre as Fases 1-3: nada crítico (sem quebra de isolamento, sem `ADMIN_SECRET` exposto, sem XSS). Corrigido: formula-injection na exportação CSV, teto silencioso de `fetchAllTasks` (agora avisa via `truncated`), falta de lockout no `ADMIN_SECRET` (agora tem, por IP). Documentado como limitação conhecida (não corrigido, baixo risco no volume atual): double-fetch entre `/admin/tasks`/`/admin/metrics`.
   - ✅ **Fase 4 (2026-08-07): primeira rota que muta a ClickUp — `POST /admin/tasks/:id`.** Status, solução (campo customizado) e atribuição de operador, com modal de gerenciamento por chamado no painel (botão "Gerenciar" em cada linha/card). Reaproveita a automação de SLA já existente (`runStatusAutomation`) sem duplicar lógica — ela dispara pelo webhook da automação da ClickUp, que reage a qualquer mudança de status, API ou UI. Testado (mock de fetch/KV + verificação end-to-end no Chromium via Playwright).
   - ✅ **Quadro (Kanban) + sidebar + drag-and-drop (2026-08-07)** — ver "Painel de admin" pra detalhes. Quadro é a visão padrão, com arrastar-e-soltar mudando status; sidebar separa Gestão de Dashboard; cores de status confirmadas direto na ClickUp via MCP.
   - 🛡️ **Auditoria do agente `revisor` (2026-08-07)** sobre a Fase 4 + Quadro/sidebar/drag-and-drop: nada crítico de segurança novo. Achado mais grave (corrigido): o modal "Gerenciar" sempre mandava `assigneeId` no `POST`, então salvar qualquer campo (ex.: só status) num chamado com 2+ operadores atribuídos apagava um deles em silêncio — agora só manda esse campo se o admin realmente tocá-lo (`operadorTouched`), e o modal avisa quando há múltiplos atribuídos. Também corrigido: validação de tipo de `solucao`/`assigneeId` no Worker (antes aceitava valor errado sem avisar), modal não assume mais "Aberto" em silêncio pra status desconhecido (avisa), cor do "Aberto" sincronizada entre `app.js`/`admin.js`, `viewMode`/grupos recolhidos da Tabela agora persistem em `localStorage`. Documentado como limitação aceita (não corrigido): as 3 sub-mutações de `POST /admin/tasks/:id` não são atômicas (sem rollback entre elas, já que cada uma é uma chamada separada à ClickUp) — em caso de falha no meio, a resposta de erro agora inclui `updated` com o que já tinha sido aplicado, pra não mascarar estado parcial.
   - ⏳ **Ainda não implementado:** exportar/reatribuir em lote (várias tasks de uma vez), histórico de quem mudou o quê (a ClickUp guarda isso nativamente, mas o painel não expõe), qualquer validação de transição de status (o painel aceita qualquer um dos 4 status a qualquer momento, igual a própria ClickUp permite pela UI dela), suporte de verdade a múltiplos operadores atribuídos no modal (hoje só pré-seleciona 1 e avisa se houver mais), e testes automatizados pro `admin.js` (busca/paginação por grupo/Kanban/drag-and-drop hoje dependem só de verificação manual via Playwright).
6. Possíveis melhorias futuras: histórico de notificações, refinamento do fluxo offline.
7. **Roadmap de modernização (D1/R2 no backend + React no frontend) — as Fases B2-B6 e F1-F4.5 estão concluídas, testadas e publicadas; F5 (frontend), B7 parte 1 e B7 parte 2 fase 1 (backend) já foram aplicadas de verdade em produção, todas em 2026-08-12.**
   - ✅ **F5 (frontend) concluída (2026-08-12):** `admin.html`/`index.html` em produção agora são os builds React de verdade — ver "Deploy do Frontend" e "Decisões técnicas tomadas" pro que foi feito e o achado real sobre `actions/deploy-pages`.
   - ✅ **B7 parte 1 (backend) concluída (2026-08-12):** espelho D1 ativo em toda escrita (create/update), `GET /api/my-tasks` já lê do D1, **453/453 chamados reais migrados** (os últimos 12, sem TIPO/SETOR, fechados no mesmo dia com uma pequena migração de schema — tipo/setor agora aceitam `NULL`). Ver "Decisões técnicas tomadas" pro detalhe completo, incluindo por que o escopo de leitura foi deliberadamente parcial.
   - ✅ **B7 parte 2 — fase 1 e fase 2 concluídas (2026-08-12):** tabela `chamado_assignees` criada e populada em produção (**454/454 chamados reais, 0 erros** — inclui backfill do 2º+ operador de quem já estava no D1 desde antes dessa tabela existir), e `GET /admin/tasks`/`GET /admin/metrics` já leem de lá (fase 2, feita na sequência a pedido do usuário na mesma sessão — não ficou pendente). Ver "Decisões técnicas tomadas" pro detalhe completo, incluindo os dois achados reais de produção (estouro de subrequests no backfill, corrigido com `.batch()`; e um 500 no deploy da fase 2 por estouro de variáveis bindáveis do D1 em resultado grande, corrigido dividindo em pedaços de 50).
   - ✅ **As duas pendências que ficaram fora de escopo da B7 parte 2 foram resolvidas pela migração completa (item 8 abaixo)**: desligar a automação de SLA da ClickUp virou `d1TransitionStatus` síncrono na Fase M4; desligar a escrita na ClickUp de vez virou D1 puro nas Fases M3-M5.
8. **🎉 Migração de saída da ClickUp CONCLUÍDA (2026-08-13, prazo era fim de agosto/2026 — terminada no mesmo dia em que começou) — 5 fases (M1-M5), plano completo salvo em `C:\Users\usuario\.claude\plans\cosmic-dazzling-pelican.md`.** Decisões já confirmadas com o usuário e cumpridas: lista de solicitantes virou tabela no D1 + tela de gestão no admin (não autocadastro livre); anexos antigos migrados todos pro R2 antes do corte, ninguém perdeu acesso; nenhuma rota de `push-worker.js` chama mais `api.clickup.com`; a conta da ClickUp fica intacta como arquivo consultável (mesmo padrão de rollback da F5), sem cancelar.
   - ✅ **M1 concluída (2026-08-13): lista de solicitantes sai da ClickUp.** Tabela `solicitantes` no D1, `GET /api/solicitantes` substitui `GET /api/field`, `handleRegister` passou a validar o nome (fechando uma lacuna de segurança real que sempre existiu), tela "Usuários" nova no admin. **43 nomes reais migrados em produção.** Ver "Decisões técnicas tomadas" pro detalhe completo.
   - ✅ **M2 concluída (2026-08-13, mesmo dia): anexos saem da ClickUp, vão pro R2.** `GET /api/anexos/:id` novo, autenticado por sessão (melhoria de segurança — URL da ClickUp era pública/direta); `GET /tasks/:id` já monta `attachments` do D1. **46 anexos reais migrados em produção, 0 erros**, via rota paginada — achado real (teto de subrequests bem menor do que os "~1000" documentados no dia anterior) corrigido reduzindo o lote padrão de 50 pra 5. Ver "Decisões técnicas tomadas" pro detalhe completo.
   - ✅ **M3 concluída (2026-08-13, mesmo dia): criar chamado vira D1-nativo.** `handleCreateTask` grava direto no D1 (`d1CreateChamado`), `id` do chamado passa a ser UUID (não mais formato ClickUp). Zero mudança de frontend precisou. Ver "Decisões técnicas tomadas" pro detalhe completo, incluindo por que a verificação em produção ficou deliberadamente limitada a auth+deploy (sem criar chamado real de teste).
   - ✅ **M4 concluída (2026-08-13, mesmo dia): status/solução/operador e automação de SLA viram síncronos no D1 — a fase mais delicada do plano.** `handleAdminUpdateTask` chama `d1TransitionStatus`/`d1UpdateChamado` direto, sem PUT/POST na ClickUp nenhum. `POST /webhook`/`handleWebhook`/`runStatusAutomation` removidos — fecha o item de segurança do `/webhook` sem autenticação.
   - ✅ **M5 concluída (2026-08-13, mesmo dia): leitura final via D1 + integração desligada — última fase.** `handleGetTask` corta pra D1 (última rota que ainda lia da ClickUp); chave de push vira `usub_<nome>` (sem mais `getSolicitanteMaps`); removidas as 3 rotas de migração de uso único (já rodaram em produção) e `GET /api/field`. **Nenhuma rota chama mais `api.clickup.com`** — confirmado por grep. `env.CLICKUP_API_KEY` deixada configurada por enquanto (remoção é opcional/futura, não bloqueante). Ver "Decisões técnicas tomadas" pro detalhe completo, incluindo o achado sobre o rollback do frontend (F5) ter ficado desatualizado por essa migração.
   - ⏳ **Únicas pendências reais, ambas fora do código**: (1) desligar manualmente, dentro da própria interface da ClickUp, a automação que ainda tenta chamar `/webhook` (sem efeito — a rota devolve 404 — mas fica como configuração órfã até alguém apagar); (2) opcionalmente rodar `wrangler secret delete CLICKUP_API_KEY` no Worker, quando quiserem reduzir a superfície (não afeta nada rodando, nenhuma rota lê esse secret mais).
9. **Painel de admin "robusto como a ClickUp" (2026-08-14) — MVP visual + Fase A + Fase B concluídos no mesmo dia.** Ver "Estado atual do desenvolvimento" pro detalhe completo de cada entrega.
   - ✅ **MVP visual** — Artifact publicado (`https://claude.ai/code/artifact/39cb6632-50a3-44ff-ae8c-8045c3ce71c7`), mockup interativo com dados fictícios, aprovado antes de qualquer código real ser tocado.
   - ✅ **Fase A** — edição inline de Status/Operador na Tabela, Setor como pill colorida, redesign da barra de filtros, Dashboard com filtro de período (de/até + atalhos).
   - ✅ **Fase B** — histórico + comentários por chamado (`chamado_eventos`, evento automático + nota manual, timeline no modal "Gerenciar"), ação em lote (`POST /admin/tasks/bulk`, seleção múltipla + barra flutuante na Tabela).
   - ⏳ **Pendente, deliberadamente adiado**: "operadores como tabela no D1" (hoje `OPERADORES` é hardcoded em `constants.ts`/`push-worker.js`, 25 usos espalhados incluindo `OPERADOR_WHATSAPP` no app do solicitante) — avaliado como blast radius alto pra um ganho que só se materializa se/quando a TI crescer além de Everson/Henrique. Vira uma tela "Operadores" no admin (mesmo padrão CRUD já usado em "Usuários" pra solicitantes) quando for priorizado.
   - ⏳ **Pendência técnica, fora do código**: rodar `POST /admin/migrate-schema-chamado-eventos` uma vez no D1 real de produção (mesmo padrão de toda migração de schema anterior) — feito manualmente depois do deploy do Worker, não faz parte do deploy automático.

> ⚠️ **Regra de commit/versão deste projeto:** nenhuma mudança (visual OU lógica) versiona/commita/pusha sozinha, **exceto durante a janela de prazo pedida pelo usuário em 2026-08-06/07** (projeto precisava estar pronto até 2026-08-07) — nesse período, comitar/publicar direto após verificar (testes + Chromium), sem pausar pra confirmar. Fora dessa janela, volta a valer agrupar em lote e só publicar quando o usuário sinalizar.
